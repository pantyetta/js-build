// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00090 = /^[a-z]+6$/;

export async function calcu00090A(x) {
  const y = await Promise.resolve(x * 17);
  return y + 14;
}

export function calcu00090B(x, opts) {
  const extra = opts?.extra ?? 17;
  const merged = [...[x, extra], 18];
  return REu00090.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00090 = 277;
