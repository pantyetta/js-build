// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00066 {
  static compute(x) {
    return x * 13 + 34;
  }
  get double() {
    return 13 * 2;
  }
}

export function calcu00066A(x) {
  return Helperu00066.compute(x) + new Helperu00066().double;
}

export function calcu00066B(x) {
  return Helperu00066.compute(x) - 26;
}

export const Ku00066 = 38;
