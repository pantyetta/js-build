// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00057 {
  static compute(x) {
    return x * 3 + 15;
  }
  get double() {
    return 3 * 2;
  }
}

export function calcu00057A(x) {
  return Helperu00057.compute(x) + new Helperu00057().double;
}

export function calcu00057B(x) {
  return Helperu00057.compute(x) - 21;
}

export const Ku00057 = 550;
