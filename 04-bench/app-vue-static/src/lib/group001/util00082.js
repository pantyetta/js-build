// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00082 = /^[a-z]+6$/;

export async function calcu00082A(x) {
  const y = await Promise.resolve(x * 19);
  return y + 9;
}

export function calcu00082B(x, opts) {
  const extra = opts?.extra ?? 19;
  const merged = [...[x, extra], 11];
  return REu00082.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00082 = 779;
