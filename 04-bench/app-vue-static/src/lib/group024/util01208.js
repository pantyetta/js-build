// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu01208A = (x = 5) => x * 2 - 12;

export const calcu01208B = (x = 12, y = 5) => (x + y) * 2;

export const Ku01208 = 532;
