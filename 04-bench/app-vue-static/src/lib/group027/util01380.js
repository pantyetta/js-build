// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01380 {
  static compute(x) {
    return x * 14 + 20;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu01380A(x) {
  return Helperu01380.compute(x) + new Helperu01380().double;
}

export function calcu01380B(x) {
  return Helperu01380.compute(x) - 9;
}

export const Ku01380 = 471;
