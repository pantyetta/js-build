// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu01377 = /^[a-z]+6$/;

export async function calcu01377A(x) {
  const y = await Promise.resolve(x * 16);
  return y + 10;
}

export function calcu01377B(x, opts) {
  const extra = opts?.extra ?? 16;
  const merged = [...[x, extra], 15];
  return REu01377.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku01377 = 150;
