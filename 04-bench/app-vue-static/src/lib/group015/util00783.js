// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00783 {
  static compute(x) {
    return x * 17 + 31;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00783A(x) {
  return Helperu00783.compute(x) + new Helperu00783().double;
}

export function calcu00783B(x) {
  return Helperu00783.compute(x) - 4;
}

export const Ku00783 = 820;
