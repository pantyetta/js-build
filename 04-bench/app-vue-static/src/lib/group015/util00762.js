// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00762 = /^[a-z]+6$/;

export async function calcu00762A(x) {
  const y = await Promise.resolve(x * 8);
  return y + 29;
}

export function calcu00762B(x, opts) {
  const extra = opts?.extra ?? 8;
  const merged = [...[x, extra], 13];
  return REu00762.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00762 = 611;
