// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00763 {
  static compute(x) {
    return x * 3 + 30;
  }
  get double() {
    return 3 * 2;
  }
}

export function calcu00763A(x) {
  return Helperu00763.compute(x) + new Helperu00763().double;
}

export function calcu00763B(x) {
  return Helperu00763.compute(x) - 23;
}

export const Ku00763 = 793;
