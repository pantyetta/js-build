// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00791 = /^[a-z]+6$/;

export async function calcu00791A(x) {
  const y = await Promise.resolve(x * 4);
  return y + 36;
}

export function calcu00791B(x, opts) {
  const extra = opts?.extra ?? 4;
  const merged = [...[x, extra], 22];
  return REu00791.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00791 = 331;
