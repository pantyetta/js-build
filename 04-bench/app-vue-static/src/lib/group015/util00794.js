// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00794 {
  static compute(x) {
    return x * 2 + 18;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00794A(x) {
  return Helperu00794.compute(x) + new Helperu00794().double;
}

export function calcu00794B(x) {
  return Helperu00794.compute(x) - 28;
}

export const Ku00794 = 17;
