// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00960A = (x = 5) => x * 4 - 23;

export const calcu00960B = (x = 23, y = 5) => (x + y) * 4;

export const Ku00960 = 162;
