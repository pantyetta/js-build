// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00959 {
  static compute(x) {
    return x * 16 + 27;
  }
  get double() {
    return 16 * 2;
  }
}

export function calcu00959A(x) {
  return Helperu00959.compute(x) + new Helperu00959().double;
}

export function calcu00959B(x) {
  return Helperu00959.compute(x) - 13;
}

export const Ku00959 = 480;
