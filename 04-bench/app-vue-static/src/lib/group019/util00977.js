// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00977 {
  static compute(x) {
    return x * 12 + 9;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00977A(x) {
  return Helperu00977.compute(x) + new Helperu00977().double;
}

export function calcu00977B(x) {
  return Helperu00977.compute(x) - 18;
}

export const Ku00977 = 110;
