// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00986 {
  static compute(x) {
    return x * 6 + 36;
  }
  get double() {
    return 6 * 2;
  }
}

export function calcu00986A(x) {
  return Helperu00986.compute(x) + new Helperu00986().double;
}

export function calcu00986B(x) {
  return Helperu00986.compute(x) - 17;
}

export const Ku00986 = 494;
