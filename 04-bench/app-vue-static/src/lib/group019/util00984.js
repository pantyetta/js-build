// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00984 {
  static compute(x) {
    return x * 8 + 7;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu00984A(x) {
  return Helperu00984.compute(x) + new Helperu00984().double;
}

export function calcu00984B(x) {
  return Helperu00984.compute(x) - 9;
}

export const Ku00984 = 174;
