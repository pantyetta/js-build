// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00968 {
  static compute(x) {
    return x * 19 + 27;
  }
  get double() {
    return 19 * 2;
  }
}

export function calcu00968A(x) {
  return Helperu00968.compute(x) + new Helperu00968().double;
}

export function calcu00968B(x) {
  return Helperu00968.compute(x) - 1;
}

export const Ku00968 = 587;
