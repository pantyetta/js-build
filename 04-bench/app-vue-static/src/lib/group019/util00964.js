// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00964 {
  static compute(x) {
    return x * 17 + 27;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00964A(x) {
  return Helperu00964.compute(x) + new Helperu00964().double;
}

export function calcu00964B(x) {
  return Helperu00964.compute(x) - 22;
}

export const Ku00964 = 240;
