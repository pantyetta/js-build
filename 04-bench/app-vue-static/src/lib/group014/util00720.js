// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00720A = (x = 4) => x * 34 - 2;

export const calcu00720B = (x = 2, y = 4) => (x + y) * 34;

export const Ku00720 = 92;
