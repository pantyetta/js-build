// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00722 {
  static compute(x) {
    return x * 14 + 36;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu00722A(x) {
  return Helperu00722.compute(x) + new Helperu00722().double;
}

export function calcu00722B(x) {
  return Helperu00722.compute(x) - 11;
}

export const Ku00722 = 466;
