// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 4 — import なしの葉モジュール（Vue 非依存）。

export function calcu00733A(x) {
  try {
    if (x < 0) throw new Error("neg");
    return x * 14 + 37;
  } catch (e) {
    return 21;
  }
}

export function calcu00733B(x) {
  switch (x % 5) {
    case 0: return x + 14;
    case 1: return x - 37;
    default: return x * 21;
  }
}

export const Ku00733 = 904;
