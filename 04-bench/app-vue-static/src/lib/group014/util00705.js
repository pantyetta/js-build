// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00705A = (x = 10) => x * 5 - 14;

export const calcu00705B = (x = 14, y = 10) => (x + y) * 5;

export const Ku00705 = 582;
