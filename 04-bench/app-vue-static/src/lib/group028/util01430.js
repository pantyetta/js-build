// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu01430A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 1;
}

export function calcu01430B(x) {
  return x * 11 - 3;
}

export const Ku01430 = 182;
