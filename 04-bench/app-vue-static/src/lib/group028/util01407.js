// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01407 {
  static compute(x) {
    return x * 18 + 37;
  }
  get double() {
    return 18 * 2;
  }
}

export function calcu01407A(x) {
  return Helperu01407.compute(x) + new Helperu01407().double;
}

export function calcu01407B(x) {
  return Helperu01407.compute(x) - 11;
}

export const Ku01407 = 488;
