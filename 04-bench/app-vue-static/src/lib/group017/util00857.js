// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00857 {
  static compute(x) {
    return x * 15 + 23;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu00857A(x) {
  return Helperu00857.compute(x) + new Helperu00857().double;
}

export function calcu00857B(x) {
  return Helperu00857.compute(x) - 5;
}

export const Ku00857 = 68;
