// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00853 {
  static compute(x) {
    return x * 18 + 24;
  }
  get double() {
    return 18 * 2;
  }
}

export function calcu00853A(x) {
  return Helperu00853.compute(x) + new Helperu00853().double;
}

export function calcu00853B(x) {
  return Helperu00853.compute(x) - 12;
}

export const Ku00853 = 457;
