// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00882 {
  static compute(x) {
    return x * 11 + 12;
  }
  get double() {
    return 11 * 2;
  }
}

export function calcu00882A(x) {
  return Helperu00882.compute(x) + new Helperu00882().double;
}

export function calcu00882B(x) {
  return Helperu00882.compute(x) - 7;
}

export const Ku00882 = 646;
