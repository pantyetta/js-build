// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00894A = (x = 3) => x * 30 - 5;

export const calcu00894B = (x = 5, y = 3) => (x + y) * 30;

export const Ku00894 = 454;
