// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00871 {
  static compute(x) {
    return x * 17 + 28;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00871A(x) {
  return Helperu00871.compute(x) + new Helperu00871().double;
}

export function calcu00871B(x) {
  return Helperu00871.compute(x) - 25;
}

export const Ku00871 = 485;
