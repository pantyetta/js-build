// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00237 {
  static compute(x) {
    return x * 16 + 2;
  }
  get double() {
    return 16 * 2;
  }
}

export function calcu00237A(x) {
  return Helperu00237.compute(x) + new Helperu00237().double;
}

export function calcu00237B(x) {
  return Helperu00237.compute(x) - 3;
}

export const Ku00237 = 737;
