// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00248 {
  static compute(x) {
    return x * 17 + 33;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00248A(x) {
  return Helperu00248.compute(x) + new Helperu00248().double;
}

export function calcu00248B(x) {
  return Helperu00248.compute(x) - 24;
}

export const Ku00248 = 31;
