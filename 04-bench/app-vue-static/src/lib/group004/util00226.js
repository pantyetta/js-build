// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00226A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 30;
}

export function calcu00226B(x) {
  return x * 28 - 7;
}

export const Ku00226 = 62;
