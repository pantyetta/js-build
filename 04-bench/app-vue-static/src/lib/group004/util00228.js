// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00228 {
  static compute(x) {
    return x * 2 + 12;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00228A(x) {
  return Helperu00228.compute(x) + new Helperu00228().double;
}

export function calcu00228B(x) {
  return Helperu00228.compute(x) - 28;
}

export const Ku00228 = 597;
