// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00216A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 36;
}

export function calcu00216B(x) {
  return x * 20 - 15;
}

export const Ku00216 = 145;
