// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00239 {
  static compute(x) {
    return x * 16 + 5;
  }
  get double() {
    return 16 * 2;
  }
}

export function calcu00239A(x) {
  return Helperu00239.compute(x) + new Helperu00239().double;
}

export function calcu00239B(x) {
  return Helperu00239.compute(x) - 28;
}

export const Ku00239 = 388;
