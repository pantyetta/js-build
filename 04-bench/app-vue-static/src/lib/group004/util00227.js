// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00227 {
  static compute(x) {
    return x * 2 + 30;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00227A(x) {
  return Helperu00227.compute(x) + new Helperu00227().double;
}

export function calcu00227B(x) {
  return Helperu00227.compute(x) - 17;
}

export const Ku00227 = 732;
