// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00221A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 1;
}

export function calcu00221B(x) {
  return x * 8 - 8;
}

export const Ku00221 = 259;
