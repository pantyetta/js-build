// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00613 {
  static compute(x) {
    return x * 17 + 14;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00613A(x) {
  return Helperu00613.compute(x) + new Helperu00613().double;
}

export function calcu00613B(x) {
  return Helperu00613.compute(x) - 26;
}

export const Ku00613 = 420;
