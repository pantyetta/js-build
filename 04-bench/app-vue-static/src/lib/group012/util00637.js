// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00637A = (x = 6) => x * 25 - 25;

export const calcu00637B = (x = 25, y = 6) => (x + y) * 25;

export const Ku00637 = 178;
