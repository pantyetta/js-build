// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00609 {
  static compute(x) {
    return x * 19 + 24;
  }
  get double() {
    return 19 * 2;
  }
}

export function calcu00609A(x) {
  return Helperu00609.compute(x) + new Helperu00609().double;
}

export function calcu00609B(x) {
  return Helperu00609.compute(x) - 22;
}

export const Ku00609 = 608;
