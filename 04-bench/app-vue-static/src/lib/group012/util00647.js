// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00647 {
  static compute(x) {
    return x * 13 + 14;
  }
  get double() {
    return 13 * 2;
  }
}

export function calcu00647A(x) {
  return Helperu00647.compute(x) + new Helperu00647().double;
}

export function calcu00647B(x) {
  return Helperu00647.compute(x) - 5;
}

export const Ku00647 = 241;
