// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00160A = (x = 5) => x * 12 - 14;

export const calcu00160B = (x = 14, y = 5) => (x + y) * 12;

export const Ku00160 = 981;
