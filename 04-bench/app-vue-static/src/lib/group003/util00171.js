// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00171 {
  static compute(x) {
    return x * 9 + 37;
  }
  get double() {
    return 9 * 2;
  }
}

export function calcu00171A(x) {
  return Helperu00171.compute(x) + new Helperu00171().double;
}

export function calcu00171B(x) {
  return Helperu00171.compute(x) - 16;
}

export const Ku00171 = 609;
