// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00159 {
  static compute(x) {
    return x * 6 + 30;
  }
  get double() {
    return 6 * 2;
  }
}

export function calcu00159A(x) {
  return Helperu00159.compute(x) + new Helperu00159().double;
}

export function calcu00159B(x) {
  return Helperu00159.compute(x) - 7;
}

export const Ku00159 = 427;
