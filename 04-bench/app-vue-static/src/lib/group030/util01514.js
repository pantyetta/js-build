// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01514 {
  static compute(x) {
    return x * 15 + 10;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu01514A(x) {
  return Helperu01514.compute(x) + new Helperu01514().double;
}

export function calcu01514B(x) {
  return Helperu01514.compute(x) - 19;
}

export const Ku01514 = 236;
