// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00042A = (x = 9) => x * 4 - 4;

export const calcu00042B = (x = 4, y = 9) => (x + y) * 4;

export const Ku00042 = 905;
