// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00033 {
  static compute(x) {
    return x * 17 + 15;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00033A(x) {
  return Helperu00033.compute(x) + new Helperu00033().double;
}

export function calcu00033B(x) {
  return Helperu00033.compute(x) - 11;
}

export const Ku00033 = 60;
