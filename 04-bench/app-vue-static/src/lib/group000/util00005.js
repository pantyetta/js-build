// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00005A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 11;
}

export function calcu00005B(x) {
  return x * 20 - 5;
}

export const Ku00005 = 480;
