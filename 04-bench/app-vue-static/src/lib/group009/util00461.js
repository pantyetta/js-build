// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00461 {
  static compute(x) {
    return x * 9 + 32;
  }
  get double() {
    return 9 * 2;
  }
}

export function calcu00461A(x) {
  return Helperu00461.compute(x) + new Helperu00461().double;
}

export function calcu00461B(x) {
  return Helperu00461.compute(x) - 19;
}

export const Ku00461 = 632;
