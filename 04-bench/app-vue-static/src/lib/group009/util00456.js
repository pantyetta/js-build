// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00456 {
  static compute(x) {
    return x * 3 + 31;
  }
  get double() {
    return 3 * 2;
  }
}

export function calcu00456A(x) {
  return Helperu00456.compute(x) + new Helperu00456().double;
}

export function calcu00456B(x) {
  return Helperu00456.compute(x) - 28;
}

export const Ku00456 = 593;
