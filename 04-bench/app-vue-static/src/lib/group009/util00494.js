// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00494A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 19;
}

export function calcu00494B(x) {
  return x * 25 - 17;
}

export const Ku00494 = 418;
