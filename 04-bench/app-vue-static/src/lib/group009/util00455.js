// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00455A = (x = 11) => x * 21 - 9;

export const calcu00455B = (x = 9, y = 11) => (x + y) * 21;

export const Ku00455 = 80;
