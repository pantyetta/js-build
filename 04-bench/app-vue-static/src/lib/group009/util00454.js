// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00454 {
  static compute(x) {
    return x * 14 + 16;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu00454A(x) {
  return Helperu00454.compute(x) + new Helperu00454().double;
}

export function calcu00454B(x) {
  return Helperu00454.compute(x) - 18;
}

export const Ku00454 = 404;
