// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00475 {
  static compute(x) {
    return x * 7 + 20;
  }
  get double() {
    return 7 * 2;
  }
}

export function calcu00475A(x) {
  return Helperu00475.compute(x) + new Helperu00475().double;
}

export function calcu00475B(x) {
  return Helperu00475.compute(x) - 3;
}

export const Ku00475 = 222;
