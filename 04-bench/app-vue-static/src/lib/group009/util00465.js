// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 4 — import なしの葉モジュール（Vue 非依存）。

export function calcu00465A(x) {
  try {
    if (x < 0) throw new Error("neg");
    return x * 6 + 34;
  } catch (e) {
    return 17;
  }
}

export function calcu00465B(x) {
  switch (x % 7) {
    case 0: return x + 6;
    case 1: return x - 34;
    default: return x * 17;
  }
}

export const Ku00465 = 124;
