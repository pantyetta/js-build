// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00497 {
  static compute(x) {
    return x * 15 + 21;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu00497A(x) {
  return Helperu00497.compute(x) + new Helperu00497().double;
}

export function calcu00497B(x) {
  return Helperu00497.compute(x) - 25;
}

export const Ku00497 = 616;
