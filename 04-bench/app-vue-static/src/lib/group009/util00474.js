// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00474 {
  static compute(x) {
    return x * 10 + 25;
  }
  get double() {
    return 10 * 2;
  }
}

export function calcu00474A(x) {
  return Helperu00474.compute(x) + new Helperu00474().double;
}

export function calcu00474B(x) {
  return Helperu00474.compute(x) - 28;
}

export const Ku00474 = 628;
