// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00466A = (x = 2) => x * 28 - 1;

export const calcu00466B = (x = 1, y = 2) => (x + y) * 28;

export const Ku00466 = 586;
