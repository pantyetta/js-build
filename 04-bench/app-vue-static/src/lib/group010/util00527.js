// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00527A = (x = 5) => x * 14 - 29;

export const calcu00527B = (x = 29, y = 5) => (x + y) * 14;

export const Ku00527 = 527;
