// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00525A = (x = 17) => x * 11 - 25;

export const calcu00525B = (x = 25, y = 17) => (x + y) * 11;

export const Ku00525 = 141;
