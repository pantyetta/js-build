// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00523A = (x = 18) => x * 22 - 18;

export const calcu00523B = (x = 18, y = 18) => (x + y) * 22;

export const Ku00523 = 106;
