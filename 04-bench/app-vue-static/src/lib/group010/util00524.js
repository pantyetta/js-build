// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00524A = (x = 4) => x * 10 - 7;

export const calcu00524B = (x = 7, y = 4) => (x + y) * 10;

export const Ku00524 = 28;
