// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00543 = /^[a-z]+6$/;

export async function calcu00543A(x) {
  const y = await Promise.resolve(x * 15);
  return y + 26;
}

export function calcu00543B(x, opts) {
  const extra = opts?.extra ?? 15;
  const merged = [...[x, extra], 29];
  return REu00543.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00543 = 830;
