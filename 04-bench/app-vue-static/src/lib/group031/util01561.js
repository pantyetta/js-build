// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu01561A = (x = 2) => x * 1 - 12;

export const calcu01561B = (x = 12, y = 2) => (x + y) * 1;

export const Ku01561 = 810;
