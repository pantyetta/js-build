// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01584 {
  static compute(x) {
    return x * 12 + 21;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu01584A(x) {
  return Helperu01584.compute(x) + new Helperu01584().double;
}

export function calcu01584B(x) {
  return Helperu01584.compute(x) - 13;
}

export const Ku01584 = 468;
