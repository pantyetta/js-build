// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00263A = (x = 7) => x * 36 - 27;

export const calcu00263B = (x = 27, y = 7) => (x + y) * 36;

export const Ku00263 = 106;
