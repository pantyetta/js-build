// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00278A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 23;
}

export function calcu00278B(x) {
  return x * 5 - 3;
}

export const Ku00278 = 80;
