// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00294 {
  static compute(x) {
    return x * 7 + 14;
  }
  get double() {
    return 7 * 2;
  }
}

export function calcu00294A(x) {
  return Helperu00294.compute(x) + new Helperu00294().double;
}

export function calcu00294B(x) {
  return Helperu00294.compute(x) - 23;
}

export const Ku00294 = 534;
