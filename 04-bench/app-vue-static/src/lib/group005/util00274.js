// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00274A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 14;
}

export function calcu00274B(x) {
  return x * 15 - 8;
}

export const Ku00274 = 981;
