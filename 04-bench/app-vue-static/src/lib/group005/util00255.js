// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00255 {
  static compute(x) {
    return x * 5 + 30;
  }
  get double() {
    return 5 * 2;
  }
}

export function calcu00255A(x) {
  return Helperu00255.compute(x) + new Helperu00255().double;
}

export function calcu00255B(x) {
  return Helperu00255.compute(x) - 28;
}

export const Ku00255 = 107;
