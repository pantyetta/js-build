// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00298A = (x = 5) => x * 14 - 1;

export const calcu00298B = (x = 1, y = 5) => (x + y) * 14;

export const Ku00298 = 687;
