// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00264A = (x = 2) => x * 35 - 22;

export const calcu00264B = (x = 22, y = 2) => (x + y) * 35;

export const Ku00264 = 965;
