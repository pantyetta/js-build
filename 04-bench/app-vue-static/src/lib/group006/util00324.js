// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00324 {
  static compute(x) {
    return x * 11 + 18;
  }
  get double() {
    return 11 * 2;
  }
}

export function calcu00324A(x) {
  return Helperu00324.compute(x) + new Helperu00324().double;
}

export function calcu00324B(x) {
  return Helperu00324.compute(x) - 29;
}

export const Ku00324 = 252;
