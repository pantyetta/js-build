// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00338 {
  static compute(x) {
    return x * 3 + 17;
  }
  get double() {
    return 3 * 2;
  }
}

export function calcu00338A(x) {
  return Helperu00338.compute(x) + new Helperu00338().double;
}

export function calcu00338B(x) {
  return Helperu00338.compute(x) - 1;
}

export const Ku00338 = 364;
