// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00319 {
  static compute(x) {
    return x * 5 + 33;
  }
  get double() {
    return 5 * 2;
  }
}

export function calcu00319A(x) {
  return Helperu00319.compute(x) + new Helperu00319().double;
}

export function calcu00319B(x) {
  return Helperu00319.compute(x) - 28;
}

export const Ku00319 = 114;
