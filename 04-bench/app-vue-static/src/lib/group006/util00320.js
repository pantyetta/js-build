// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00320A = (x = 17) => x * 4 - 2;

export const calcu00320B = (x = 2, y = 17) => (x + y) * 4;

export const Ku00320 = 237;
