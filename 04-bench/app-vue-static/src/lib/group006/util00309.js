// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00309 = /^[a-z]+6$/;

export async function calcu00309A(x) {
  const y = await Promise.resolve(x * 11);
  return y + 34;
}

export function calcu00309B(x, opts) {
  const extra = opts?.extra ?? 11;
  const merged = [...[x, extra], 8];
  return REu00309.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00309 = 253;
