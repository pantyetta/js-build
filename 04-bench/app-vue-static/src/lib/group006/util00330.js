// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00330 {
  static compute(x) {
    return x * 5 + 9;
  }
  get double() {
    return 5 * 2;
  }
}

export function calcu00330A(x) {
  return Helperu00330.compute(x) + new Helperu00330().double;
}

export function calcu00330B(x) {
  return Helperu00330.compute(x) - 16;
}

export const Ku00330 = 806;
