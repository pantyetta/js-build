// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00316 {
  static compute(x) {
    return x * 3 + 35;
  }
  get double() {
    return 3 * 2;
  }
}

export function calcu00316A(x) {
  return Helperu00316.compute(x) + new Helperu00316().double;
}

export function calcu00316B(x) {
  return Helperu00316.compute(x) - 9;
}

export const Ku00316 = 496;
