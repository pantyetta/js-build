// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00377A = (x = 16) => x * 28 - 2;

export const calcu00377B = (x = 2, y = 16) => (x + y) * 28;

export const Ku00377 = 804;
