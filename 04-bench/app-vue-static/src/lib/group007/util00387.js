// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール（Vue 非依存）。

const REu00387 = /^[a-z]+6$/;

export async function calcu00387A(x) {
  const y = await Promise.resolve(x * 4);
  return y + 11;
}

export function calcu00387B(x, opts) {
  const extra = opts?.extra ?? 4;
  const merged = [...[x, extra], 12];
  return REu00387.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00387 = 312;
