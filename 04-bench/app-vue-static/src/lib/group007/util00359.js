// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00359A = (x = 4) => x * 20 - 25;

export const calcu00359B = (x = 25, y = 4) => (x + y) * 20;

export const Ku00359 = 928;
