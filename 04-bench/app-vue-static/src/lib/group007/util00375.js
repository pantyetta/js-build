// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu00375A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 5;
}

export function calcu00375B(x) {
  return x * 4 - 19;
}

export const Ku00375 = 238;
