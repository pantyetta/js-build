// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00808 {
  static compute(x) {
    return x * 2 + 32;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00808A(x) {
  return Helperu00808.compute(x) + new Helperu00808().double;
}

export function calcu00808B(x) {
  return Helperu00808.compute(x) - 10;
}

export const Ku00808 = 951;
