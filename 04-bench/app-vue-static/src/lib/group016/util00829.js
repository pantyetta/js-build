// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00829 {
  static compute(x) {
    return x * 12 + 28;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00829A(x) {
  return Helperu00829.compute(x) + new Helperu00829().double;
}

export function calcu00829B(x) {
  return Helperu00829.compute(x) - 28;
}

export const Ku00829 = 137;
