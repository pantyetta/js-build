// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00833 {
  static compute(x) {
    return x * 19 + 22;
  }
  get double() {
    return 19 * 2;
  }
}

export function calcu00833A(x) {
  return Helperu00833.compute(x) + new Helperu00833().double;
}

export function calcu00833B(x) {
  return Helperu00833.compute(x) - 24;
}

export const Ku00833 = 417;
