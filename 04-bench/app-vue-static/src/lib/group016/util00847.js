// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00847 {
  static compute(x) {
    return x * 12 + 12;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00847A(x) {
  return Helperu00847.compute(x) + new Helperu00847().double;
}

export function calcu00847B(x) {
  return Helperu00847.compute(x) - 18;
}

export const Ku00847 = 426;
