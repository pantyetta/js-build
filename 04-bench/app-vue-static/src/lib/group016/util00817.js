// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00817 {
  static compute(x) {
    return x * 12 + 7;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00817A(x) {
  return Helperu00817.compute(x) + new Helperu00817().double;
}

export function calcu00817B(x) {
  return Helperu00817.compute(x) - 7;
}

export const Ku00817 = 259;
