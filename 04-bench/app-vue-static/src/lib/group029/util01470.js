// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01470 {
  static compute(x) {
    return x * 10 + 33;
  }
  get double() {
    return 10 * 2;
  }
}

export function calcu01470A(x) {
  return Helperu01470.compute(x) + new Helperu01470().double;
}

export function calcu01470B(x) {
  return Helperu01470.compute(x) - 18;
}

export const Ku01470 = 798;
