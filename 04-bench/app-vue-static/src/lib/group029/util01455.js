// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01455 {
  static compute(x) {
    return x * 13 + 33;
  }
  get double() {
    return 13 * 2;
  }
}

export function calcu01455A(x) {
  return Helperu01455.compute(x) + new Helperu01455().double;
}

export function calcu01455B(x) {
  return Helperu01455.compute(x) - 29;
}

export const Ku01455 = 685;
