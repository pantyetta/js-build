// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール（Vue 非依存）。

export function calcu01478A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 19;
}

export function calcu01478B(x) {
  return x * 7 - 13;
}

export const Ku01478 = 192;
