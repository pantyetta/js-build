// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu01496 {
  static compute(x) {
    return x * 4 + 37;
  }
  get double() {
    return 4 * 2;
  }
}

export function calcu01496A(x) {
  return Helperu01496.compute(x) + new Helperu01496().double;
}

export function calcu01496B(x) {
  return Helperu01496.compute(x) - 6;
}

export const Ku01496 = 821;
