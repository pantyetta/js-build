// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00654 {
  static compute(x) {
    return x * 11 + 35;
  }
  get double() {
    return 11 * 2;
  }
}

export function calcu00654A(x) {
  return Helperu00654.compute(x) + new Helperu00654().double;
}

export function calcu00654B(x) {
  return Helperu00654.compute(x) - 26;
}

export const Ku00654 = 383;
