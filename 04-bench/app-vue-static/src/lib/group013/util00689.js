// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00689 {
  static compute(x) {
    return x * 6 + 34;
  }
  get double() {
    return 6 * 2;
  }
}

export function calcu00689A(x) {
  return Helperu00689.compute(x) + new Helperu00689().double;
}

export function calcu00689B(x) {
  return Helperu00689.compute(x) - 26;
}

export const Ku00689 = 881;
