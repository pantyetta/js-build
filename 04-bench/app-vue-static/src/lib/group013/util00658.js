// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00658 {
  static compute(x) {
    return x * 17 + 13;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00658A(x) {
  return Helperu00658.compute(x) + new Helperu00658().double;
}

export function calcu00658B(x) {
  return Helperu00658.compute(x) - 12;
}

export const Ku00658 = 931;
