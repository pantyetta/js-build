// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00936 {
  static compute(x) {
    return x * 8 + 33;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu00936A(x) {
  return Helperu00936.compute(x) + new Helperu00936().double;
}

export function calcu00936B(x) {
  return Helperu00936.compute(x) - 10;
}

export const Ku00936 = 246;
