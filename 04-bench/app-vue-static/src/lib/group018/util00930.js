// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00930 {
  static compute(x) {
    return x * 16 + 37;
  }
  get double() {
    return 16 * 2;
  }
}

export function calcu00930A(x) {
  return Helperu00930.compute(x) + new Helperu00930().double;
}

export function calcu00930B(x) {
  return Helperu00930.compute(x) - 26;
}

export const Ku00930 = 578;
