// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール（Vue 非依存）。

export const calcu00927A = (x = 12) => x * 21 - 12;

export const calcu00927B = (x = 12, y = 12) => (x + y) * 21;

export const Ku00927 = 83;
