// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00447 {
  static compute(x) {
    return x * 11 + 22;
  }
  get double() {
    return 11 * 2;
  }
}

export function calcu00447A(x) {
  return Helperu00447.compute(x) + new Helperu00447().double;
}

export function calcu00447B(x) {
  return Helperu00447.compute(x) - 6;
}

export const Ku00447 = 927;
