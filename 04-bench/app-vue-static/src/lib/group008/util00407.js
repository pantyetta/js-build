// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00407 {
  static compute(x) {
    return x * 10 + 29;
  }
  get double() {
    return 10 * 2;
  }
}

export function calcu00407A(x) {
  return Helperu00407.compute(x) + new Helperu00407().double;
}

export function calcu00407B(x) {
  return Helperu00407.compute(x) - 21;
}

export const Ku00407 = 991;
