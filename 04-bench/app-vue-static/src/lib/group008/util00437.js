// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00437 {
  static compute(x) {
    return x * 14 + 11;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu00437A(x) {
  return Helperu00437.compute(x) + new Helperu00437().double;
}

export function calcu00437B(x) {
  return Helperu00437.compute(x) - 11;
}

export const Ku00437 = 702;
