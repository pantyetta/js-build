// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00444 {
  static compute(x) {
    return x * 5 + 3;
  }
  get double() {
    return 5 * 2;
  }
}

export function calcu00444A(x) {
  return Helperu00444.compute(x) + new Helperu00444().double;
}

export function calcu00444B(x) {
  return Helperu00444.compute(x) - 7;
}

export const Ku00444 = 539;
