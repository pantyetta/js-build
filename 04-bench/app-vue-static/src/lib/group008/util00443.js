// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00443 {
  static compute(x) {
    return x * 13 + 30;
  }
  get double() {
    return 13 * 2;
  }
}

export function calcu00443A(x) {
  return Helperu00443.compute(x) + new Helperu00443().double;
}

export function calcu00443B(x) {
  return Helperu00443.compute(x) - 16;
}

export const Ku00443 = 751;
