// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00588 {
  static compute(x) {
    return x * 3 + 37;
  }
  get double() {
    return 3 * 2;
  }
}

export function calcu00588A(x) {
  return Helperu00588.compute(x) + new Helperu00588().double;
}

export function calcu00588B(x) {
  return Helperu00588.compute(x) - 6;
}

export const Ku00588 = 309;
