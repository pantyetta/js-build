// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00598 {
  static compute(x) {
    return x * 17 + 36;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00598A(x) {
  return Helperu00598.compute(x) + new Helperu00598().double;
}

export function calcu00598B(x) {
  return Helperu00598.compute(x) - 13;
}

export const Ku00598 = 308;
