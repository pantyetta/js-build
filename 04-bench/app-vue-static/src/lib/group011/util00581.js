// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00581 {
  static compute(x) {
    return x * 9 + 31;
  }
  get double() {
    return 9 * 2;
  }
}

export function calcu00581A(x) {
  return Helperu00581.compute(x) + new Helperu00581().double;
}

export function calcu00581B(x) {
  return Helperu00581.compute(x) - 16;
}

export const Ku00581 = 124;
