// 生成ファイル（04-bench/generate-vue.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール（Vue 非依存）。

class Helperu00592 {
  static compute(x) {
    return x * 14 + 16;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu00592A(x) {
  return Helperu00592.compute(x) + new Helperu00592().double;
}

export function calcu00592B(x) {
  return Helperu00592.compute(x) - 19;
}

export const Ku00592 = 466;
