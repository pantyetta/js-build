// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00636 {
  static compute(x) {
    return x * 2 + 35;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00636A(x) {
  return Helperu00636.compute(x) + new Helperu00636().double;
}

export function calcu00636B(x) {
  return Helperu00636.compute(x) - 25;
}

export const Ku00636 = 133;
