// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00622A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 14;
}

export function calcu00622B(x) {
  return x * 20 - 19;
}

export const Ku00622 = 995;
