// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00632 {
  static compute(x) {
    return x * 12 + 35;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00632A(x) {
  return Helperu00632.compute(x) + new Helperu00632().double;
}

export function calcu00632B(x) {
  return Helperu00632.compute(x) - 19;
}

export const Ku00632 = 518;
