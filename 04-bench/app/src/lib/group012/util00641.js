// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00641A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 6;
}

export function calcu00641B(x) {
  return x * 29 - 12;
}

export const Ku00641 = 227;
