// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01488A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 22;
}

export function calcu01488B(x) {
  return x * 12 - 19;
}

export const Ku01488 = 420;
