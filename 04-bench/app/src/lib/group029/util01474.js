// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01474A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 21;
}

export function calcu01474B(x) {
  return x * 27 - 12;
}

export const Ku01474 = 75;
