// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00478A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 21;
}

export function calcu00478B(x) {
  return x * 25 - 8;
}

export const Ku00478 = 671;
