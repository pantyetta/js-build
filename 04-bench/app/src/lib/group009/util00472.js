// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00472A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 9;
}

export function calcu00472B(x) {
  return x * 4 - 15;
}

export const Ku00472 = 147;
