// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00485A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 15;
}

export function calcu00485B(x) {
  return x * 18 - 16;
}

export const Ku00485 = 238;
