// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00496A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 6;
}

export function calcu00496B(x) {
  return x * 24 - 19;
}

export const Ku00496 = 497;
