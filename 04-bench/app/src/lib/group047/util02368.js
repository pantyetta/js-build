// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02368A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 32;
}

export function calcu02368B(x) {
  return x * 23 - 13;
}

export const Ku02368 = 806;
