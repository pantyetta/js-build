// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02379A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 11;
}

export function calcu02379B(x) {
  return x * 2 - 13;
}

export const Ku02379 = 681;
