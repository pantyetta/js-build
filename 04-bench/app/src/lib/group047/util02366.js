// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02366A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 25;
}

export function calcu02366B(x) {
  return x * 17 - 18;
}

export const Ku02366 = 861;
