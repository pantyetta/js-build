// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02389 = /^[a-z]+6$/;

export async function calcu02389A(x) {
  const y = await Promise.resolve(x * 8);
  return y + 16;
}

export function calcu02389B(x, opts) {
  const extra = opts?.extra ?? 8;
  const merged = [...[x, extra], 29];
  return REu02389.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02389 = 235;
