// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02097A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 16;
}

export function calcu02097B(x) {
  return x * 13 - 2;
}

export const Ku02097 = 369;
