// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02095A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 25;
}

export function calcu02095B(x) {
  return x * 26 - 14;
}

export const Ku02095 = 197;
