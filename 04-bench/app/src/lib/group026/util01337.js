// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01337A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 31;
}

export function calcu01337B(x) {
  return x * 11 - 2;
}

export const Ku01337 = 160;
