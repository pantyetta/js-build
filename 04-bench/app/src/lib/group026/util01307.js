// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01307A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 15;
}

export function calcu01307B(x) {
  return x * 25 - 8;
}

export const Ku01307 = 369;
