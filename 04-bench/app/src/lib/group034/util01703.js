// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01703A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 30;
}

export function calcu01703B(x) {
  return x * 22 - 9;
}

export const Ku01703 = 68;
