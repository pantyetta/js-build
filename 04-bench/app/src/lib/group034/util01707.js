// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01707A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 10;
}

export function calcu01707B(x) {
  return x * 19 - 3;
}

export const Ku01707 = 489;
