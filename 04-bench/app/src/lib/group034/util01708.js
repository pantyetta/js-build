// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01708A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 31;
}

export function calcu01708B(x) {
  return x * 6 - 7;
}

export const Ku01708 = 133;
