// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00088A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 16;
}

export function calcu00088B(x) {
  return x * 18 - 4;
}

export const Ku00088 = 313;
