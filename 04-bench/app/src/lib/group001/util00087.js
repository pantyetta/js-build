// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00087A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 4;
}

export function calcu00087B(x) {
  return x * 12 - 15;
}

export const Ku00087 = 637;
