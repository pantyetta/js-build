// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00076A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 16;
}

export function calcu00076B(x) {
  return x * 5 - 14;
}

export const Ku00076 = 876;
