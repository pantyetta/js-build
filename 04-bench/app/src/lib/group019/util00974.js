// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00974 = /^[a-z]+6$/;

export async function calcu00974A(x) {
  const y = await Promise.resolve(x * 17);
  return y + 26;
}

export function calcu00974B(x, opts) {
  const extra = opts?.extra ?? 17;
  const merged = [...[x, extra], 5];
  return REu00974.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00974 = 420;
