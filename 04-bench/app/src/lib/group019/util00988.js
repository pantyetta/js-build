// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00988A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 28;
}

export function calcu00988B(x) {
  return x * 1 - 12;
}

export const Ku00988 = 482;
