// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00998A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 36;
}

export function calcu00998B(x) {
  return x * 2 - 5;
}

export const Ku00998 = 153;
