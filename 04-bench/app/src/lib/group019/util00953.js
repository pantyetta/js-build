// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00953A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 16;
}

export function calcu00953B(x) {
  return x * 10 - 16;
}

export const Ku00953 = 371;
