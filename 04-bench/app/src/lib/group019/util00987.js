// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00987 = /^[a-z]+6$/;

export async function calcu00987A(x) {
  const y = await Promise.resolve(x * 16);
  return y + 2;
}

export function calcu00987B(x, opts) {
  const extra = opts?.extra ?? 16;
  const merged = [...[x, extra], 25];
  return REu00987.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00987 = 486;
