// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00979A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 27;
}

export function calcu00979B(x) {
  return x * 17 - 4;
}

export const Ku00979 = 647;
