// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00965A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 20;
}

export function calcu00965B(x) {
  return x * 15 - 17;
}

export const Ku00965 = 264;
