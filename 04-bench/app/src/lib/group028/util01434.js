// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu01434 {
  static compute(x) {
    return x * 2 + 16;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu01434A(x) {
  return Helperu01434.compute(x) + new Helperu01434().double;
}

export function calcu01434B(x) {
  return Helperu01434.compute(x) - 13;
}

export const Ku01434 = 585;
