// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01424A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 35;
}

export function calcu01424B(x) {
  return x * 9 - 6;
}

export const Ku01424 = 425;
