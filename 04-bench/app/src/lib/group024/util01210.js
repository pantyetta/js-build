// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01210A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 20;
}

export function calcu01210B(x) {
  return x * 16 - 11;
}

export const Ku01210 = 566;
