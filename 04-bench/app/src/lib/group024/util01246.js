// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01246A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 37;
}

export function calcu01246B(x) {
  return x * 4 - 6;
}

export const Ku01246 = 517;
