// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00687A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 18;
}

export function calcu00687B(x) {
  return x * 20 - 6;
}

export const Ku00687 = 182;
