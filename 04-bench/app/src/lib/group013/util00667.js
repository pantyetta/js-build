// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00667A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 35;
}

export function calcu00667B(x) {
  return x * 27 - 18;
}

export const Ku00667 = 866;
