// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00438A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 33;
}

export function calcu00438B(x) {
  return x * 25 - 11;
}

export const Ku00438 = 162;
