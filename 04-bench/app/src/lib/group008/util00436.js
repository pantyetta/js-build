// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00436A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 3;
}

export function calcu00436B(x) {
  return x * 25 - 12;
}

export const Ku00436 = 778;
