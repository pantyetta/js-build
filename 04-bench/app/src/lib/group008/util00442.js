// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00442A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 32;
}

export function calcu00442B(x) {
  return x * 25 - 8;
}

export const Ku00442 = 78;
