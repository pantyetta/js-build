// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00243A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 23;
}

export function calcu00243B(x) {
  return x * 23 - 16;
}

export const Ku00243 = 886;
