// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00206A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 29;
}

export function calcu00206B(x) {
  return x * 17 - 7;
}

export const Ku00206 = 328;
