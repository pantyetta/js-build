// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00209A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 7;
}

export function calcu00209B(x) {
  return x * 7 - 16;
}

export const Ku00209 = 910;
