// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02634 = /^[a-z]+6$/;

export async function calcu02634A(x) {
  const y = await Promise.resolve(x * 4);
  return y + 37;
}

export function calcu02634B(x, opts) {
  const extra = opts?.extra ?? 4;
  const merged = [...[x, extra], 15];
  return REu02634.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02634 = 897;
