// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02624A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 6;
}

export function calcu02624B(x) {
  return x * 7 - 7;
}

export const Ku02624 = 779;
