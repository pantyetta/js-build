// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02646 = /^[a-z]+6$/;

export async function calcu02646A(x) {
  const y = await Promise.resolve(x * 3);
  return y + 33;
}

export function calcu02646B(x, opts) {
  const extra = opts?.extra ?? 3;
  const merged = [...[x, extra], 15];
  return REu02646.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02646 = 26;
