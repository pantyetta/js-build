// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02637A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 24;
}

export function calcu02637B(x) {
  return x * 27 - 3;
}

export const Ku02637 = 170;
