// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01812A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 9;
}

export function calcu01812B(x) {
  return x * 28 - 10;
}

export const Ku01812 = 296;
