// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01800A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 1;
}

export function calcu01800B(x) {
  return x * 21 - 2;
}

export const Ku01800 = 620;
