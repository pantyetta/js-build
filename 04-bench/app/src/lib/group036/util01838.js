// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01838A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 2;
}

export function calcu01838B(x) {
  return x * 13 - 13;
}

export const Ku01838 = 700;
