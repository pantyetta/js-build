// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01602A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 8;
}

export function calcu01602B(x) {
  return x * 23 - 4;
}

export const Ku01602 = 93;
