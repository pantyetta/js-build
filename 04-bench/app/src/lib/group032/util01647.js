// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01647A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 37;
}

export function calcu01647B(x) {
  return x * 28 - 3;
}

export const Ku01647 = 854;
