// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01646A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 23;
}

export function calcu01646B(x) {
  return x * 19 - 14;
}

export const Ku01646 = 910;
