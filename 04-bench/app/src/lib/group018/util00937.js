// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00937 {
  static compute(x) {
    return x * 18 + 8;
  }
  get double() {
    return 18 * 2;
  }
}

export function calcu00937A(x) {
  return Helperu00937.compute(x) + new Helperu00937().double;
}

export function calcu00937B(x) {
  return Helperu00937.compute(x) - 27;
}

export const Ku00937 = 579;
