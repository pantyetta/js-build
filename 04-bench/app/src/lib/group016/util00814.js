// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00814 = /^[a-z]+6$/;

export async function calcu00814A(x) {
  const y = await Promise.resolve(x * 17);
  return y + 24;
}

export function calcu00814B(x, opts) {
  const extra = opts?.extra ?? 17;
  const merged = [...[x, extra], 15];
  return REu00814.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00814 = 711;
