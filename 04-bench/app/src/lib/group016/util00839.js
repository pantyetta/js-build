// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00839A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 13;
}

export function calcu00839B(x) {
  return x * 17 - 19;
}

export const Ku00839 = 972;
