// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00848 {
  static compute(x) {
    return x * 7 + 3;
  }
  get double() {
    return 7 * 2;
  }
}

export function calcu00848A(x) {
  return Helperu00848.compute(x) + new Helperu00848().double;
}

export function calcu00848B(x) {
  return Helperu00848.compute(x) - 22;
}

export const Ku00848 = 158;
