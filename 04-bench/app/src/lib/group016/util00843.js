// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00843 {
  static compute(x) {
    return x * 8 + 4;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu00843A(x) {
  return Helperu00843.compute(x) + new Helperu00843().double;
}

export function calcu00843B(x) {
  return Helperu00843.compute(x) - 14;
}

export const Ku00843 = 129;
