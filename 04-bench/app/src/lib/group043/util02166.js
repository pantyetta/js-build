// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02166A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 12;
}

export function calcu02166B(x) {
  return x * 4 - 10;
}

export const Ku02166 = 944;
