// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02197A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 35;
}

export function calcu02197B(x) {
  return x * 27 - 4;
}

export const Ku02197 = 103;
