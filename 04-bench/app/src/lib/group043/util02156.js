// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02156A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 2;
}

export function calcu02156B(x) {
  return x * 7 - 18;
}

export const Ku02156 = 539;
