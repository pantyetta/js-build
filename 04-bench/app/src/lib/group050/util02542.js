// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02542A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 8;
}

export function calcu02542B(x) {
  return x * 17 - 10;
}

export const Ku02542 = 165;
