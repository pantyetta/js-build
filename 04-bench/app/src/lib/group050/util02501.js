// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02501A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 3;
}

export function calcu02501B(x) {
  return x * 1 - 17;
}

export const Ku02501 = 984;
