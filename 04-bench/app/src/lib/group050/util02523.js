// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02523A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 28;
}

export function calcu02523B(x) {
  return x * 11 - 15;
}

export const Ku02523 = 660;
