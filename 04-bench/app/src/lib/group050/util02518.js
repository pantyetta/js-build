// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02518A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 11;
}

export function calcu02518B(x) {
  return x * 11 - 17;
}

export const Ku02518 = 936;
