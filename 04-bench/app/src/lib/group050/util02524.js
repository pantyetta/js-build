// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02524A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 15;
}

export function calcu02524B(x) {
  return x * 2 - 5;
}

export const Ku02524 = 943;
