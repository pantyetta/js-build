// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02701A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 7;
}

export function calcu02701B(x) {
  return x * 27 - 4;
}

export const Ku02701 = 91;
