// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02737A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 11;
}

export function calcu02737B(x) {
  return x * 17 - 16;
}

export const Ku02737 = 230;
