// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01851A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 31;
}

export function calcu01851B(x) {
  return x * 12 - 10;
}

export const Ku01851 = 492;
