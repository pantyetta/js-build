// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01886A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 33;
}

export function calcu01886B(x) {
  return x * 23 - 12;
}

export const Ku01886 = 997;
