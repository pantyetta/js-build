// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01896A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 9;
}

export function calcu01896B(x) {
  return x * 2 - 14;
}

export const Ku01896 = 981;
