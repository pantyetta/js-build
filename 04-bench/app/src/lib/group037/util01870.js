// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01870A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 21;
}

export function calcu01870B(x) {
  return x * 3 - 4;
}

export const Ku01870 = 895;
