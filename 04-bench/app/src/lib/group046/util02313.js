// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02313 = /^[a-z]+6$/;

export async function calcu02313A(x) {
  const y = await Promise.resolve(x * 8);
  return y + 9;
}

export function calcu02313B(x, opts) {
  const extra = opts?.extra ?? 8;
  const merged = [...[x, extra], 20];
  return REu02313.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02313 = 916;
