// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02331A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 16;
}

export function calcu02331B(x) {
  return x * 14 - 5;
}

export const Ku02331 = 669;
