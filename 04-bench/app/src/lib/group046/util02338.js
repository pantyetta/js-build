// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02338A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 19;
}

export function calcu02338B(x) {
  return x * 23 - 7;
}

export const Ku02338 = 649;
