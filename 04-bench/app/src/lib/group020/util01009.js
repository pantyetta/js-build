// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01009A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 24;
}

export function calcu01009B(x) {
  return x * 29 - 7;
}

export const Ku01009 = 857;
