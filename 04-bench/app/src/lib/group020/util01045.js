// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01045A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 1;
}

export function calcu01045B(x) {
  return x * 15 - 11;
}

export const Ku01045 = 393;
