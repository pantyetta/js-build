// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01012A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 10;
}

export function calcu01012B(x) {
  return x * 6 - 2;
}

export const Ku01012 = 155;
