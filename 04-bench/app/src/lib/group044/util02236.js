// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02236A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 14;
}

export function calcu02236B(x) {
  return x * 5 - 4;
}

export const Ku02236 = 354;
