// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02213A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 20;
}

export function calcu02213B(x) {
  return x * 4 - 17;
}

export const Ku02213 = 743;
