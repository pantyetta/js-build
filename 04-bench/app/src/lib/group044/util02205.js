// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02205A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 22;
}

export function calcu02205B(x) {
  return x * 21 - 13;
}

export const Ku02205 = 611;
