// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02034A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 15;
}

export function calcu02034B(x) {
  return x * 26 - 15;
}

export const Ku02034 = 465;
