// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02015A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 5;
}

export function calcu02015B(x) {
  return x * 25 - 18;
}

export const Ku02015 = 173;
