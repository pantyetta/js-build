// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02006A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 15;
}

export function calcu02006B(x) {
  return x * 24 - 2;
}

export const Ku02006 = 660;
