// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02950A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 2;
}

export function calcu02950B(x) {
  return x * 17 - 2;
}

export const Ku02950 = 105;
