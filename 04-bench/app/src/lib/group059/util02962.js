// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02962A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 1;
}

export function calcu02962B(x) {
  return x * 2 - 12;
}

export const Ku02962 = 385;
