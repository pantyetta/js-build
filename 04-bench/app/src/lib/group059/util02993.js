// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02993 {
  static compute(x) {
    return x * 15 + 18;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu02993A(x) {
  return Helperu02993.compute(x) + new Helperu02993().double;
}

export function calcu02993B(x) {
  return Helperu02993.compute(x) - 27;
}

export const Ku02993 = 294;
