// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02966A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 27;
}

export function calcu02966B(x) {
  return x * 13 - 3;
}

export const Ku02966 = 363;
