// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01549A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 37;
}

export function calcu01549B(x) {
  return x * 6 - 14;
}

export const Ku01549 = 826;
