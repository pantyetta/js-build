// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02264A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 28;
}

export function calcu02264B(x) {
  return x * 28 - 3;
}

export const Ku02264 = 808;
