// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02259A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 18;
}

export function calcu02259B(x) {
  return x * 3 - 16;
}

export const Ku02259 = 973;
