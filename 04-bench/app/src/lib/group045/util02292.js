// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02292 = /^[a-z]+6$/;

export async function calcu02292A(x) {
  const y = await Promise.resolve(x * 12);
  return y + 35;
}

export function calcu02292B(x, opts) {
  const extra = opts?.extra ?? 12;
  const merged = [...[x, extra], 8];
  return REu02292.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02292 = 513;
