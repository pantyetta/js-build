// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02279A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 32;
}

export function calcu02279B(x) {
  return x * 13 - 2;
}

export const Ku02279 = 539;
