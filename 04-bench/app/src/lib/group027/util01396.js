// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01396A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 8;
}

export function calcu01396B(x) {
  return x * 11 - 12;
}

export const Ku01396 = 400;
