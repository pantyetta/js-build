// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01350A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 5;
}

export function calcu01350B(x) {
  return x * 1 - 13;
}

export const Ku01350 = 316;
