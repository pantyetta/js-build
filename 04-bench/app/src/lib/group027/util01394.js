// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01394A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 33;
}

export function calcu01394B(x) {
  return x * 18 - 5;
}

export const Ku01394 = 817;
