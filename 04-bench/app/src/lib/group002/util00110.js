// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00110A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 19;
}

export function calcu00110B(x) {
  return x * 13 - 5;
}

export const Ku00110 = 499;
