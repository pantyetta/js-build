// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01761A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 28;
}

export function calcu01761B(x) {
  return x * 29 - 14;
}

export const Ku01761 = 245;
