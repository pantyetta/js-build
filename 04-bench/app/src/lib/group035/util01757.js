// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01757A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 34;
}

export function calcu01757B(x) {
  return x * 19 - 19;
}

export const Ku01757 = 102;
