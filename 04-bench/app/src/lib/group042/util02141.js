// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02141A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 14;
}

export function calcu02141B(x) {
  return x * 26 - 18;
}

export const Ku02141 = 415;
