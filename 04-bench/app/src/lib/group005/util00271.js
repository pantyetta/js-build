// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00271A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 25;
}

export function calcu00271B(x) {
  return x * 29 - 11;
}

export const Ku00271 = 690;
