// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00295 {
  static compute(x) {
    return x * 9 + 3;
  }
  get double() {
    return 9 * 2;
  }
}

export function calcu00295A(x) {
  return Helperu00295.compute(x) + new Helperu00295().double;
}

export function calcu00295B(x) {
  return Helperu00295.compute(x) - 27;
}

export const Ku00295 = 515;
