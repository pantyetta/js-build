// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00283A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 9;
}

export function calcu00283B(x) {
  return x * 6 - 12;
}

export const Ku00283 = 6;
