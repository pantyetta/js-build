// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00253A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 32;
}

export function calcu00253B(x) {
  return x * 11 - 5;
}

export const Ku00253 = 292;
