// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01981A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 27;
}

export function calcu01981B(x) {
  return x * 10 - 8;
}

export const Ku01981 = 780;
