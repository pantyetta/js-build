// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01983A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 25;
}

export function calcu01983B(x) {
  return x * 8 - 19;
}

export const Ku01983 = 23;
