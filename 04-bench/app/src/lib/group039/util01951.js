// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01951A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 3;
}

export function calcu01951B(x) {
  return x * 1 - 16;
}

export const Ku01951 = 664;
