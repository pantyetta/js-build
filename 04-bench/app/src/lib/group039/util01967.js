// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu01967 {
  static compute(x) {
    return x * 17 + 29;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu01967A(x) {
  return Helperu01967.compute(x) + new Helperu01967().double;
}

export function calcu01967B(x) {
  return Helperu01967.compute(x) - 5;
}

export const Ku01967 = 911;
