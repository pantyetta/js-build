// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01956A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 12;
}

export function calcu01956B(x) {
  return x * 5 - 15;
}

export const Ku01956 = 847;
