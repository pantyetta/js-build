// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01271A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 25;
}

export function calcu01271B(x) {
  return x * 16 - 18;
}

export const Ku01271 = 834;
