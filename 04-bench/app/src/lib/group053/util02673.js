// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02673A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 19;
}

export function calcu02673B(x) {
  return x * 2 - 9;
}

export const Ku02673 = 162;
