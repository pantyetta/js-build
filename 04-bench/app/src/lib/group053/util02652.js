// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02652A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 8;
}

export function calcu02652B(x) {
  return x * 27 - 5;
}

export const Ku02652 = 659;
