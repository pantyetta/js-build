// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02575A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 18;
}

export function calcu02575B(x) {
  return x * 2 - 6;
}

export const Ku02575 = 211;
