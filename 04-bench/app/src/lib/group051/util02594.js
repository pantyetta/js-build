// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02594 = /^[a-z]+6$/;

export async function calcu02594A(x) {
  const y = await Promise.resolve(x * 13);
  return y + 15;
}

export function calcu02594B(x, opts) {
  const extra = opts?.extra ?? 13;
  const merged = [...[x, extra], 9];
  return REu02594.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02594 = 817;
