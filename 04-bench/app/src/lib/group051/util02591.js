// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02591A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 15;
}

export function calcu02591B(x) {
  return x * 27 - 15;
}

export const Ku02591 = 477;
