// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02593A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 24;
}

export function calcu02593B(x) {
  return x * 6 - 11;
}

export const Ku02593 = 840;
