// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02568 = /^[a-z]+6$/;

export async function calcu02568A(x) {
  const y = await Promise.resolve(x * 15);
  return y + 8;
}

export function calcu02568B(x, opts) {
  const extra = opts?.extra ?? 15;
  const merged = [...[x, extra], 14];
  return REu02568.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02568 = 433;
