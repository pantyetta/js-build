// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02408A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 16;
}

export function calcu02408B(x) {
  return x * 12 - 13;
}

export const Ku02408 = 215;
