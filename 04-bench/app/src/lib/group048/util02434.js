// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02434 = /^[a-z]+6$/;

export async function calcu02434A(x) {
  const y = await Promise.resolve(x * 9);
  return y + 36;
}

export function calcu02434B(x, opts) {
  const extra = opts?.extra ?? 9;
  const merged = [...[x, extra], 22];
  return REu02434.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02434 = 291;
