// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02436A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 21;
}

export function calcu02436B(x) {
  return x * 25 - 14;
}

export const Ku02436 = 505;
