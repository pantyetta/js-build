// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02443 = /^[a-z]+6$/;

export async function calcu02443A(x) {
  const y = await Promise.resolve(x * 11);
  return y + 29;
}

export function calcu02443B(x, opts) {
  const extra = opts?.extra ?? 11;
  const merged = [...[x, extra], 8];
  return REu02443.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02443 = 973;
