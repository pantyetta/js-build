// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02402A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 35;
}

export function calcu02402B(x) {
  return x * 2 - 2;
}

export const Ku02402 = 254;
