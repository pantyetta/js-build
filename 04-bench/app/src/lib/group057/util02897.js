// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02897 {
  static compute(x) {
    return x * 4 + 16;
  }
  get double() {
    return 4 * 2;
  }
}

export function calcu02897A(x) {
  return Helperu02897.compute(x) + new Helperu02897().double;
}

export function calcu02897B(x) {
  return Helperu02897.compute(x) - 18;
}

export const Ku02897 = 996;
