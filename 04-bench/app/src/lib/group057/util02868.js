// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02868 = /^[a-z]+6$/;

export async function calcu02868A(x) {
  const y = await Promise.resolve(x * 3);
  return y + 27;
}

export function calcu02868B(x, opts) {
  const extra = opts?.extra ?? 3;
  const merged = [...[x, extra], 8];
  return REu02868.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02868 = 665;
