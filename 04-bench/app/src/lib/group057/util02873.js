// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02873A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 3;
}

export function calcu02873B(x) {
  return x * 24 - 19;
}

export const Ku02873 = 801;
