// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02899A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 14;
}

export function calcu02899B(x) {
  return x * 12 - 17;
}

export const Ku02899 = 350;
