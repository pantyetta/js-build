// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00515 = /^[a-z]+6$/;

export async function calcu00515A(x) {
  const y = await Promise.resolve(x * 2);
  return y + 14;
}

export function calcu00515B(x, opts) {
  const extra = opts?.extra ?? 2;
  const merged = [...[x, extra], 11];
  return REu00515.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00515 = 339;
