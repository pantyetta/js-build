// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00536 {
  static compute(x) {
    return x * 16 + 37;
  }
  get double() {
    return 16 * 2;
  }
}

export function calcu00536A(x) {
  return Helperu00536.compute(x) + new Helperu00536().double;
}

export function calcu00536B(x) {
  return Helperu00536.compute(x) - 27;
}

export const Ku00536 = 544;
