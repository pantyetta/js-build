// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01558A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 27;
}

export function calcu01558B(x) {
  return x * 1 - 7;
}

export const Ku01558 = 39;
