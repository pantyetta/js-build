// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01560A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 30;
}

export function calcu01560B(x) {
  return x * 26 - 4;
}

export const Ku01560 = 263;
