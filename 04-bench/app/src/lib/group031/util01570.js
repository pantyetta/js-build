// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01570A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 24;
}

export function calcu01570B(x) {
  return x * 7 - 6;
}

export const Ku01570 = 398;
