// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01582A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 2;
}

export function calcu01582B(x) {
  return x * 5 - 9;
}

export const Ku01582 = 660;
