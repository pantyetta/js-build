// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu01596 {
  static compute(x) {
    return x * 14 + 32;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu01596A(x) {
  return Helperu01596.compute(x) + new Helperu01596().double;
}

export function calcu01596B(x) {
  return Helperu01596.compute(x) - 7;
}

export const Ku01596 = 494;
