// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02929A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 13;
}

export function calcu02929B(x) {
  return x * 9 - 2;
}

export const Ku02929 = 626;
