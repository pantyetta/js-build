// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02944A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 33;
}

export function calcu02944B(x) {
  return x * 26 - 7;
}

export const Ku02944 = 594;
