// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01193A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 18;
}

export function calcu01193B(x) {
  return x * 11 - 10;
}

export const Ku01193 = 459;
