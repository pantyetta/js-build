// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01196A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 19;
}

export function calcu01196B(x) {
  return x * 14 - 15;
}

export const Ku01196 = 239;
