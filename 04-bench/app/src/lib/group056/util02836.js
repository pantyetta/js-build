// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02836 = /^[a-z]+6$/;

export async function calcu02836A(x) {
  const y = await Promise.resolve(x * 15);
  return y + 25;
}

export function calcu02836B(x, opts) {
  const extra = opts?.extra ?? 15;
  const merged = [...[x, extra], 4];
  return REu02836.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02836 = 515;
