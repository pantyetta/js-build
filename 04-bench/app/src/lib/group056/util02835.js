// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02835A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 28;
}

export function calcu02835B(x) {
  return x * 17 - 2;
}

export const Ku02835 = 854;
