// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02833 = /^[a-z]+6$/;

export async function calcu02833A(x) {
  const y = await Promise.resolve(x * 2);
  return y + 30;
}

export function calcu02833B(x, opts) {
  const extra = opts?.extra ?? 2;
  const merged = [...[x, extra], 17];
  return REu02833.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02833 = 575;
