// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02497A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 33;
}

export function calcu02497B(x) {
  return x * 16 - 17;
}

export const Ku02497 = 44;
