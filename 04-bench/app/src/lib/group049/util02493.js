// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02493A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 31;
}

export function calcu02493B(x) {
  return x * 21 - 14;
}

export const Ku02493 = 567;
