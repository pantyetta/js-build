// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02451A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 6;
}

export function calcu02451B(x) {
  return x * 17 - 15;
}

export const Ku02451 = 336;
