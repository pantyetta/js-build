// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02452A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 28;
}

export function calcu02452B(x) {
  return x * 28 - 19;
}

export const Ku02452 = 136;
