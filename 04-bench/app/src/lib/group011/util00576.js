// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00576A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 30;
}

export function calcu00576B(x) {
  return x * 2 - 15;
}

export const Ku00576 = 728;
