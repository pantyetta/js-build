// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00569 {
  static compute(x) {
    return x * 18 + 24;
  }
  get double() {
    return 18 * 2;
  }
}

export function calcu00569A(x) {
  return Helperu00569.compute(x) + new Helperu00569().double;
}

export function calcu00569B(x) {
  return Helperu00569.compute(x) - 25;
}

export const Ku00569 = 457;
