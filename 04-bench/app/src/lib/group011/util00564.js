// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00564A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 32;
}

export function calcu00564B(x) {
  return x * 21 - 19;
}

export const Ku00564 = 902;
