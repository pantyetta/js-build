// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00594A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 24;
}

export function calcu00594B(x) {
  return x * 2 - 13;
}

export const Ku00594 = 444;
