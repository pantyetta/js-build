// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00556 {
  static compute(x) {
    return x * 4 + 18;
  }
  get double() {
    return 4 * 2;
  }
}

export function calcu00556A(x) {
  return Helperu00556.compute(x) + new Helperu00556().double;
}

export function calcu00556B(x) {
  return Helperu00556.compute(x) - 2;
}

export const Ku00556 = 872;
