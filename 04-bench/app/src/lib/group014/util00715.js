// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00715 {
  static compute(x) {
    return x * 7 + 33;
  }
  get double() {
    return 7 * 2;
  }
}

export function calcu00715A(x) {
  return Helperu00715.compute(x) + new Helperu00715().double;
}

export function calcu00715B(x) {
  return Helperu00715.compute(x) - 18;
}

export const Ku00715 = 909;
