// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00721A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 11;
}

export function calcu00721B(x) {
  return x * 12 - 5;
}

export const Ku00721 = 99;
