// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00723A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 5;
}

export function calcu00723B(x) {
  return x * 10 - 15;
}

export const Ku00723 = 538;
