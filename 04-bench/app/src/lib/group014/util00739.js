// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00739 = /^[a-z]+6$/;

export async function calcu00739A(x) {
  const y = await Promise.resolve(x * 2);
  return y + 36;
}

export function calcu00739B(x, opts) {
  const extra = opts?.extra ?? 2;
  const merged = [...[x, extra], 3];
  return REu00739.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00739 = 743;
