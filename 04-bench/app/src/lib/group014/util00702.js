// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00702A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 16;
}

export function calcu00702B(x) {
  return x * 4 - 18;
}

export const Ku00702 = 719;
