// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00727 {
  static compute(x) {
    return x * 8 + 35;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu00727A(x) {
  return Helperu00727.compute(x) + new Helperu00727().double;
}

export function calcu00727B(x) {
  return Helperu00727.compute(x) - 3;
}

export const Ku00727 = 107;
