// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00355 {
  static compute(x) {
    return x * 15 + 33;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu00355A(x) {
  return Helperu00355.compute(x) + new Helperu00355().double;
}

export function calcu00355B(x) {
  return Helperu00355.compute(x) - 5;
}

export const Ku00355 = 535;
