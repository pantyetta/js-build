// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00399 = /^[a-z]+6$/;

export async function calcu00399A(x) {
  const y = await Promise.resolve(x * 9);
  return y + 37;
}

export function calcu00399B(x, opts) {
  const extra = opts?.extra ?? 9;
  const merged = [...[x, extra], 8];
  return REu00399.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00399 = 439;
