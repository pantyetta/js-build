// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02794 {
  static compute(x) {
    return x * 18 + 32;
  }
  get double() {
    return 18 * 2;
  }
}

export function calcu02794A(x) {
  return Helperu02794.compute(x) + new Helperu02794().double;
}

export function calcu02794B(x) {
  return Helperu02794.compute(x) - 17;
}

export const Ku02794 = 609;
