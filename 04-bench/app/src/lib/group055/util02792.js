// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02792 = /^[a-z]+6$/;

export async function calcu02792A(x) {
  const y = await Promise.resolve(x * 2);
  return y + 31;
}

export function calcu02792B(x, opts) {
  const extra = opts?.extra ?? 2;
  const merged = [...[x, extra], 23];
  return REu02792.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02792 = 552;
