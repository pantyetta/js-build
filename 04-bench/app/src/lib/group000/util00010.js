// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 1 — import なしの葉モジュール。

export const calcu00010A = (x = 13) => x * 10 - 16;

export const calcu00010B = (x = 16, y = 13) => (x + y) * 10;

export const Ku00010 = 157;
