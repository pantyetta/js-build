// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00046 {
  static compute(x) {
    return x * 2 + 14;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00046A(x) {
  return Helperu00046.compute(x) + new Helperu00046().double;
}

export function calcu00046B(x) {
  return Helperu00046.compute(x) - 7;
}

export const Ku00046 = 672;
