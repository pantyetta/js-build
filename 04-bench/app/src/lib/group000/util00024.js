// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00024A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 12;
}

export function calcu00024B(x) {
  return x * 9 - 13;
}

export const Ku00024 = 103;
