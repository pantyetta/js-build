// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02322A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 1;
}

export function calcu02322B(x) {
  return x * 21 - 8;
}

export const Ku02322 = 156;
