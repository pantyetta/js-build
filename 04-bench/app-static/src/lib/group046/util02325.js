// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02325A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 28;
}

export function calcu02325B(x) {
  return x * 17 - 6;
}

export const Ku02325 = 736;
