// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02340A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 5;
}

export function calcu02340B(x) {
  return x * 28 - 5;
}

export const Ku02340 = 319;
