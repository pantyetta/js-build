// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02336A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 11;
}

export function calcu02336B(x) {
  return x * 20 - 5;
}

export const Ku02336 = 551;
