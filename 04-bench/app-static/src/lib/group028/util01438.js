// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01438A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 4;
}

export function calcu01438B(x) {
  return x * 11 - 11;
}

export const Ku01438 = 128;
