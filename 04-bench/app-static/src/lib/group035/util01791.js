// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01791A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 29;
}

export function calcu01791B(x) {
  return x * 14 - 6;
}

export const Ku01791 = 269;
