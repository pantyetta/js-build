// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu01777 {
  static compute(x) {
    return x * 4 + 5;
  }
  get double() {
    return 4 * 2;
  }
}

export function calcu01777A(x) {
  return Helperu01777.compute(x) + new Helperu01777().double;
}

export function calcu01777B(x) {
  return Helperu01777.compute(x) - 20;
}

export const Ku01777 = 670;
