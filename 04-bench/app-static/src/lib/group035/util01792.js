// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01792A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 11;
}

export function calcu01792B(x) {
  return x * 8 - 2;
}

export const Ku01792 = 453;
