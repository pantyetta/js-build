// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01651A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 13;
}

export function calcu01651B(x) {
  return x * 20 - 8;
}

export const Ku01651 = 953;
