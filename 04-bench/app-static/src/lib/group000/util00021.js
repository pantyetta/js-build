// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00021A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 12;
}

export function calcu00021B(x) {
  return x * 10 - 18;
}

export const Ku00021 = 97;
