// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02184A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 30;
}

export function calcu02184B(x) {
  return x * 8 - 14;
}

export const Ku02184 = 147;
