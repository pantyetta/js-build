// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02174 = /^[a-z]+6$/;

export async function calcu02174A(x) {
  const y = await Promise.resolve(x * 7);
  return y + 32;
}

export function calcu02174B(x, opts) {
  const extra = opts?.extra ?? 7;
  const merged = [...[x, extra], 17];
  return REu02174.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02174 = 252;
