// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02165A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 15;
}

export function calcu02165B(x) {
  return x * 20 - 11;
}

export const Ku02165 = 460;
