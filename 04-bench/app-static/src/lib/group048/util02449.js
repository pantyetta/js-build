// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02449 = /^[a-z]+6$/;

export async function calcu02449A(x) {
  const y = await Promise.resolve(x * 9);
  return y + 28;
}

export function calcu02449B(x, opts) {
  const extra = opts?.extra ?? 9;
  const merged = [...[x, extra], 25];
  return REu02449.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02449 = 176;
