// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02446A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 27;
}

export function calcu02446B(x) {
  return x * 1 - 6;
}

export const Ku02446 = 111;
