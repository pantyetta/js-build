// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02427A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 21;
}

export function calcu02427B(x) {
  return x * 27 - 3;
}

export const Ku02427 = 76;
