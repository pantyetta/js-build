// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00297 {
  static compute(x) {
    return x * 2 + 13;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00297A(x) {
  return Helperu00297.compute(x) + new Helperu00297().double;
}

export function calcu00297B(x) {
  return Helperu00297.compute(x) - 22;
}

export const Ku00297 = 968;
