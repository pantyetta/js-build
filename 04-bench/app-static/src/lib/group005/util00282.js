// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00282A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 14;
}

export function calcu00282B(x) {
  return x * 16 - 12;
}

export const Ku00282 = 829;
