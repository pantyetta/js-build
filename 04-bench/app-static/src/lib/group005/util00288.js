// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00288 = /^[a-z]+6$/;

export async function calcu00288A(x) {
  const y = await Promise.resolve(x * 4);
  return y + 11;
}

export function calcu00288B(x, opts) {
  const extra = opts?.extra ?? 4;
  const merged = [...[x, extra], 8];
  return REu00288.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00288 = 815;
