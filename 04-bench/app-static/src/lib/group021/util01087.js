// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01087A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 35;
}

export function calcu01087B(x) {
  return x * 11 - 12;
}

export const Ku01087 = 992;
