// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01060A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 6;
}

export function calcu01060B(x) {
  return x * 24 - 10;
}

export const Ku01060 = 678;
