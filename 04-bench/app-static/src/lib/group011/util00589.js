// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00589A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 19;
}

export function calcu00589B(x) {
  return x * 9 - 6;
}

export const Ku00589 = 564;
