// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00573A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 9;
}

export function calcu00573B(x) {
  return x * 10 - 14;
}

export const Ku00573 = 883;
