// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00565 {
  static compute(x) {
    return x * 7 + 25;
  }
  get double() {
    return 7 * 2;
  }
}

export function calcu00565A(x) {
  return Helperu00565.compute(x) + new Helperu00565().double;
}

export function calcu00565B(x) {
  return Helperu00565.compute(x) - 23;
}

export const Ku00565 = 356;
