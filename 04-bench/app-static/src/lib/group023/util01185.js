// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01185A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 13;
}

export function calcu01185B(x) {
  return x * 13 - 6;
}

export const Ku01185 = 242;
