// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02795A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 29;
}

export function calcu02795B(x) {
  return x * 6 - 16;
}

export const Ku02795 = 59;
