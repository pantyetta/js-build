// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02788 = /^[a-z]+6$/;

export async function calcu02788A(x) {
  const y = await Promise.resolve(x * 4);
  return y + 13;
}

export function calcu02788B(x, opts) {
  const extra = opts?.extra ?? 4;
  const merged = [...[x, extra], 22];
  return REu02788.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02788 = 421;
