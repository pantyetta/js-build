// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02780A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 9;
}

export function calcu02780B(x) {
  return x * 28 - 15;
}

export const Ku02780 = 317;
