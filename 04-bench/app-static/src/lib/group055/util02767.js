// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02767A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 5;
}

export function calcu02767B(x) {
  return x * 16 - 16;
}

export const Ku02767 = 258;
