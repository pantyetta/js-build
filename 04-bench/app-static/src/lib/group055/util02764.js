// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02764A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 24;
}

export function calcu02764B(x) {
  return x * 20 - 12;
}

export const Ku02764 = 14;
