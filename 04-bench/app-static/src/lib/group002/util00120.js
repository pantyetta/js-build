// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00120 = /^[a-z]+6$/;

export async function calcu00120A(x) {
  const y = await Promise.resolve(x * 10);
  return y + 36;
}

export function calcu00120B(x, opts) {
  const extra = opts?.extra ?? 10;
  const merged = [...[x, extra], 18];
  return REu00120.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00120 = 690;
