// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00131A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 27;
}

export function calcu00131B(x) {
  return x * 29 - 10;
}

export const Ku00131 = 628;
