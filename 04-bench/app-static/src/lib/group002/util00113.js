// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00113A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 5;
}

export function calcu00113B(x) {
  return x * 9 - 3;
}

export const Ku00113 = 280;
