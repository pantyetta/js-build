// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02651A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 20;
}

export function calcu02651B(x) {
  return x * 15 - 4;
}

export const Ku02651 = 744;
