// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02658 {
  static compute(x) {
    return x * 8 + 26;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu02658A(x) {
  return Helperu02658.compute(x) + new Helperu02658().double;
}

export function calcu02658B(x) {
  return Helperu02658.compute(x) - 14;
}

export const Ku02658 = 486;
