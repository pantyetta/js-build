// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01618A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 35;
}

export function calcu01618B(x) {
  return x * 4 - 16;
}

export const Ku01618 = 288;
