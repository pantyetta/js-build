// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01630A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 6;
}

export function calcu01630B(x) {
  return x * 4 - 14;
}

export const Ku01630 = 4;
