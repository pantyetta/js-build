// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01607A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 25;
}

export function calcu01607B(x) {
  return x * 14 - 11;
}

export const Ku01607 = 819;
