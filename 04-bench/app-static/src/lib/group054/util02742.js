// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02742A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 14;
}

export function calcu02742B(x) {
  return x * 13 - 11;
}

export const Ku02742 = 327;
