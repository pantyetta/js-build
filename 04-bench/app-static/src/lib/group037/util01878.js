// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01878A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 18;
}

export function calcu01878B(x) {
  return x * 27 - 11;
}

export const Ku01878 = 915;
