// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01874A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 33;
}

export function calcu01874B(x) {
  return x * 4 - 4;
}

export const Ku01874 = 348;
