// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02574A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 12;
}

export function calcu02574B(x) {
  return x * 12 - 7;
}

export const Ku02574 = 638;
