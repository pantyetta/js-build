// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00165A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 24;
}

export function calcu00165B(x) {
  return x * 22 - 9;
}

export const Ku00165 = 33;
