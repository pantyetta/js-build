// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00164A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 27;
}

export function calcu00164B(x) {
  return x * 21 - 18;
}

export const Ku00164 = 978;
