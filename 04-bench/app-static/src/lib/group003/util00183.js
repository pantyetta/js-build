// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00183A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 37;
}

export function calcu00183B(x) {
  return x * 25 - 15;
}

export const Ku00183 = 784;
