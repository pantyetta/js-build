// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00168 {
  static compute(x) {
    return x * 17 + 34;
  }
  get double() {
    return 17 * 2;
  }
}

export function calcu00168A(x) {
  return Helperu00168.compute(x) + new Helperu00168().double;
}

export function calcu00168B(x) {
  return Helperu00168.compute(x) - 18;
}

export const Ku00168 = 690;
