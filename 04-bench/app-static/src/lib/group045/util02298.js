// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02298A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 2;
}

export function calcu02298B(x) {
  return x * 29 - 14;
}

export const Ku02298 = 392;
