// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02035A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 27;
}

export function calcu02035B(x) {
  return x * 28 - 15;
}

export const Ku02035 = 384;
