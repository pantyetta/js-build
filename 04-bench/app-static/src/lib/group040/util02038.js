// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02038A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 17;
}

export function calcu02038B(x) {
  return x * 24 - 12;
}

export const Ku02038 = 88;
