// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02000A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 4;
}

export function calcu02000B(x) {
  return x * 14 - 17;
}

export const Ku02000 = 667;
