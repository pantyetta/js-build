// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu01457 {
  static compute(x) {
    return x * 8 + 35;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu01457A(x) {
  return Helperu01457.compute(x) + new Helperu01457().double;
}

export function calcu01457B(x) {
  return Helperu01457.compute(x) - 1;
}

export const Ku01457 = 562;
