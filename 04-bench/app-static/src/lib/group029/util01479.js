// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01479A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 1;
}

export function calcu01479B(x) {
  return x * 23 - 5;
}

export const Ku01479 = 994;
