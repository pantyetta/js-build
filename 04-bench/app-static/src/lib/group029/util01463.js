// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01463A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 16;
}

export function calcu01463B(x) {
  return x * 10 - 19;
}

export const Ku01463 = 313;
