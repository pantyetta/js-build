// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02096A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 19;
}

export function calcu02096B(x) {
  return x * 2 - 18;
}

export const Ku02096 = 445;
