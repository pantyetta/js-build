// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02246A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 29;
}

export function calcu02246B(x) {
  return x * 22 - 10;
}

export const Ku02246 = 421;
