// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02240A(x) {
  const label = `leaf-block`;
  return label.length + x * 14 + 18;
}

export function calcu02240B(x) {
  return x * 2 - 14;
}

export const Ku02240 = 436;
