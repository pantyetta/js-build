// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02228A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 12;
}

export function calcu02228B(x) {
  return x * 20 - 18;
}

export const Ku02228 = 142;
