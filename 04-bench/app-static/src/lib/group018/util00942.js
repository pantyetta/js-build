// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00942A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 8;
}

export function calcu00942B(x) {
  return x * 21 - 6;
}

export const Ku00942 = 633;
