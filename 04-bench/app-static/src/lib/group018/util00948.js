// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00948A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 21;
}

export function calcu00948B(x) {
  return x * 14 - 13;
}

export const Ku00948 = 430;
