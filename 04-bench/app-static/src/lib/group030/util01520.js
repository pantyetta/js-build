// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01520A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 7;
}

export function calcu01520B(x) {
  return x * 6 - 15;
}

export const Ku01520 = 727;
