// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02457A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 26;
}

export function calcu02457B(x) {
  return x * 19 - 16;
}

export const Ku02457 = 847;
