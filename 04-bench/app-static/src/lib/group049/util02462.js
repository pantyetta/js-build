// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02462A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 20;
}

export function calcu02462B(x) {
  return x * 18 - 16;
}

export const Ku02462 = 778;
