// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02487A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 16;
}

export function calcu02487B(x) {
  return x * 15 - 4;
}

export const Ku02487 = 496;
