// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00492 = /^[a-z]+6$/;

export async function calcu00492A(x) {
  const y = await Promise.resolve(x * 12);
  return y + 32;
}

export function calcu00492B(x, opts) {
  const extra = opts?.extra ?? 12;
  const merged = [...[x, extra], 18];
  return REu00492.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00492 = 804;
