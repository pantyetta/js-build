// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00484 = /^[a-z]+6$/;

export async function calcu00484A(x) {
  const y = await Promise.resolve(x * 15);
  return y + 35;
}

export function calcu00484B(x, opts) {
  const extra = opts?.extra ?? 15;
  const merged = [...[x, extra], 26];
  return REu00484.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00484 = 381;
