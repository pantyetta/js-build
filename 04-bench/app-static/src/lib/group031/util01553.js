// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01553A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 34;
}

export function calcu01553B(x) {
  return x * 15 - 13;
}

export const Ku01553 = 321;
