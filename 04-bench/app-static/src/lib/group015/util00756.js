// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00756 {
  static compute(x) {
    return x * 6 + 15;
  }
  get double() {
    return 6 * 2;
  }
}

export function calcu00756A(x) {
  return Helperu00756.compute(x) + new Helperu00756().double;
}

export function calcu00756B(x) {
  return Helperu00756.compute(x) - 18;
}

export const Ku00756 = 395;
