// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00789 {
  static compute(x) {
    return x * 12 + 14;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00789A(x) {
  return Helperu00789.compute(x) + new Helperu00789().double;
}

export function calcu00789B(x) {
  return Helperu00789.compute(x) - 15;
}

export const Ku00789 = 830;
