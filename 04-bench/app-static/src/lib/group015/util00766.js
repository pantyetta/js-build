// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00766 {
  static compute(x) {
    return x * 18 + 11;
  }
  get double() {
    return 18 * 2;
  }
}

export function calcu00766A(x) {
  return Helperu00766.compute(x) + new Helperu00766().double;
}

export function calcu00766B(x) {
  return Helperu00766.compute(x) - 21;
}

export const Ku00766 = 984;
