// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00758 {
  static compute(x) {
    return x * 10 + 27;
  }
  get double() {
    return 10 * 2;
  }
}

export function calcu00758A(x) {
  return Helperu00758.compute(x) + new Helperu00758().double;
}

export function calcu00758B(x) {
  return Helperu00758.compute(x) - 17;
}

export const Ku00758 = 16;
