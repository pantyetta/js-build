// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00785A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 32;
}

export function calcu00785B(x) {
  return x * 21 - 5;
}

export const Ku00785 = 588;
