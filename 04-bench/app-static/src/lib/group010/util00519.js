// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00519A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 6;
}

export function calcu00519B(x) {
  return x * 20 - 7;
}

export const Ku00519 = 341;
