// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02375 = /^[a-z]+6$/;

export async function calcu02375A(x) {
  const y = await Promise.resolve(x * 8);
  return y + 7;
}

export function calcu02375B(x, opts) {
  const extra = opts?.extra ?? 8;
  const merged = [...[x, extra], 27];
  return REu02375.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02375 = 15;
