// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02383 {
  static compute(x) {
    return x * 2 + 20;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu02383A(x) {
  return Helperu02383.compute(x) + new Helperu02383().double;
}

export function calcu02383B(x) {
  return Helperu02383.compute(x) - 19;
}

export const Ku02383 = 776;
