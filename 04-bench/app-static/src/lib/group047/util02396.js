// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02396A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 20;
}

export function calcu02396B(x) {
  return x * 4 - 16;
}

export const Ku02396 = 815;
