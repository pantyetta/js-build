// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02356A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 36;
}

export function calcu02356B(x) {
  return x * 22 - 12;
}

export const Ku02356 = 160;
