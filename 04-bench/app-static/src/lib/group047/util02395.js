// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02395 = /^[a-z]+6$/;

export async function calcu02395A(x) {
  const y = await Promise.resolve(x * 8);
  return y + 21;
}

export function calcu02395B(x, opts) {
  const extra = opts?.extra ?? 8;
  const merged = [...[x, extra], 15];
  return REu02395.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02395 = 956;
