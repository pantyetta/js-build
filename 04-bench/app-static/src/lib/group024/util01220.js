// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01220A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 22;
}

export function calcu01220B(x) {
  return x * 1 - 7;
}

export const Ku01220 = 858;
