// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01202A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 35;
}

export function calcu01202B(x) {
  return x * 2 - 11;
}

export const Ku01202 = 187;
