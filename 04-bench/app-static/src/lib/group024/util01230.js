// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01230A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 30;
}

export function calcu01230B(x) {
  return x * 21 - 19;
}

export const Ku01230 = 518;
