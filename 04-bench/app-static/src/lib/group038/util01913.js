// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01913A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 11;
}

export function calcu01913B(x) {
  return x * 12 - 3;
}

export const Ku01913 = 125;
