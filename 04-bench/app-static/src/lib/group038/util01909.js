// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01909A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 14;
}

export function calcu01909B(x) {
  return x * 10 - 6;
}

export const Ku01909 = 44;
