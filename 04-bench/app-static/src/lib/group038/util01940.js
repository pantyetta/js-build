// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01940A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 17;
}

export function calcu01940B(x) {
  return x * 22 - 13;
}

export const Ku01940 = 653;
