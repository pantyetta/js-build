// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01938A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 1;
}

export function calcu01938B(x) {
  return x * 10 - 7;
}

export const Ku01938 = 727;
