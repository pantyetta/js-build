// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01905A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 26;
}

export function calcu01905B(x) {
  return x * 16 - 6;
}

export const Ku01905 = 336;
