// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01937A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 23;
}

export function calcu01937B(x) {
  return x * 1 - 9;
}

export const Ku01937 = 397;
