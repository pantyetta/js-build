// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02103A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 19;
}

export function calcu02103B(x) {
  return x * 2 - 5;
}

export const Ku02103 = 617;
