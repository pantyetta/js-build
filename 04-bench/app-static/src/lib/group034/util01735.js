// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01735A(x) {
  const label = `leaf-block`;
  return label.length + x * 10 + 35;
}

export function calcu01735B(x) {
  return x * 5 - 10;
}

export const Ku01735 = 183;
