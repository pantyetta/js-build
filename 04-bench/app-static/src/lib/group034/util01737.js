// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01737A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 8;
}

export function calcu01737B(x) {
  return x * 21 - 9;
}

export const Ku01737 = 239;
