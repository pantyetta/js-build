// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01734A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 30;
}

export function calcu01734B(x) {
  return x * 20 - 17;
}

export const Ku01734 = 985;
