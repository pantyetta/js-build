// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01712A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 37;
}

export function calcu01712B(x) {
  return x * 15 - 13;
}

export const Ku01712 = 273;
