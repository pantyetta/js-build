// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00726A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 33;
}

export function calcu00726B(x) {
  return x * 16 - 2;
}

export const Ku00726 = 9;
