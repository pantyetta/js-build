// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00741 = /^[a-z]+6$/;

export async function calcu00741A(x) {
  const y = await Promise.resolve(x * 8);
  return y + 32;
}

export function calcu00741B(x, opts) {
  const extra = opts?.extra ?? 8;
  const merged = [...[x, extra], 3];
  return REu00741.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00741 = 97;
