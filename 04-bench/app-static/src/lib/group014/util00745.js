// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00745A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 27;
}

export function calcu00745B(x) {
  return x * 25 - 2;
}

export const Ku00745 = 755;
