// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00743 = /^[a-z]+6$/;

export async function calcu00743A(x) {
  const y = await Promise.resolve(x * 11);
  return y + 8;
}

export function calcu00743B(x, opts) {
  const extra = opts?.extra ?? 11;
  const merged = [...[x, extra], 12];
  return REu00743.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00743 = 591;
