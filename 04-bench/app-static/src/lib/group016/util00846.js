// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00846 {
  static compute(x) {
    return x * 12 + 23;
  }
  get double() {
    return 12 * 2;
  }
}

export function calcu00846A(x) {
  return Helperu00846.compute(x) + new Helperu00846().double;
}

export function calcu00846B(x) {
  return Helperu00846.compute(x) - 5;
}

export const Ku00846 = 703;
