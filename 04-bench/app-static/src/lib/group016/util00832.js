// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00832 {
  static compute(x) {
    return x * 2 + 14;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00832A(x) {
  return Helperu00832.compute(x) + new Helperu00832().double;
}

export function calcu00832B(x) {
  return Helperu00832.compute(x) - 26;
}

export const Ku00832 = 289;
