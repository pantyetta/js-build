// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00805A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 18;
}

export function calcu00805B(x) {
  return x * 6 - 13;
}

export const Ku00805 = 341;
