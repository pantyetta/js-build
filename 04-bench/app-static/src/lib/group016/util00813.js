// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00813A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 25;
}

export function calcu00813B(x) {
  return x * 18 - 15;
}

export const Ku00813 = 674;
