// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00849A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 22;
}

export function calcu00849B(x) {
  return x * 22 - 13;
}

export const Ku00849 = 820;
