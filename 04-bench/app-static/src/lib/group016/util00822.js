// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00822A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 32;
}

export function calcu00822B(x) {
  return x * 6 - 3;
}

export const Ku00822 = 87;
