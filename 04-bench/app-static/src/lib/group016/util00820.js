// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00820A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 19;
}

export function calcu00820B(x) {
  return x * 27 - 11;
}

export const Ku00820 = 763;
