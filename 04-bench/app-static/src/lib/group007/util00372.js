// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00372 {
  static compute(x) {
    return x * 6 + 7;
  }
  get double() {
    return 6 * 2;
  }
}

export function calcu00372A(x) {
  return Helperu00372.compute(x) + new Helperu00372().double;
}

export function calcu00372B(x) {
  return Helperu00372.compute(x) - 15;
}

export const Ku00372 = 972;
