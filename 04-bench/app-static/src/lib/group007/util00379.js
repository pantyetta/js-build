// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00379A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 22;
}

export function calcu00379B(x) {
  return x * 8 - 3;
}

export const Ku00379 = 912;
