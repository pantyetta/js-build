// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00368A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 14;
}

export function calcu00368B(x) {
  return x * 2 - 9;
}

export const Ku00368 = 722;
