// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00383 {
  static compute(x) {
    return x * 15 + 23;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu00383A(x) {
  return Helperu00383.compute(x) + new Helperu00383().double;
}

export function calcu00383B(x) {
  return Helperu00383.compute(x) - 26;
}

export const Ku00383 = 785;
