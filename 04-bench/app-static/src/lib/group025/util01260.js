// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01260A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 3;
}

export function calcu01260B(x) {
  return x * 9 - 4;
}

export const Ku01260 = 853;
