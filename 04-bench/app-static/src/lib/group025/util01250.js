// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01250A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 10;
}

export function calcu01250B(x) {
  return x * 21 - 15;
}

export const Ku01250 = 889;
