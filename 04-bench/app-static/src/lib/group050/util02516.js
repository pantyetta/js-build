// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02516A(x) {
  const label = `leaf-block`;
  return label.length + x * 3 + 3;
}

export function calcu02516B(x) {
  return x * 15 - 3;
}

export const Ku02516 = 748;
