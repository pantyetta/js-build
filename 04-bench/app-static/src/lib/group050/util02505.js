// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02505A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 16;
}

export function calcu02505B(x) {
  return x * 23 - 8;
}

export const Ku02505 = 86;
