// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02522 = /^[a-z]+6$/;

export async function calcu02522A(x) {
  const y = await Promise.resolve(x * 14);
  return y + 21;
}

export function calcu02522B(x, opts) {
  const extra = opts?.extra ?? 14;
  const merged = [...[x, extra], 13];
  return REu02522.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02522 = 549;
