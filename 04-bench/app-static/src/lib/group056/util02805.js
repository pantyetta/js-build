// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02805A(x) {
  const label = `leaf-block`;
  return label.length + x * 11 + 32;
}

export function calcu02805B(x) {
  return x * 8 - 11;
}

export const Ku02805 = 4;
