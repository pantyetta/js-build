// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02846 = /^[a-z]+6$/;

export async function calcu02846A(x) {
  const y = await Promise.resolve(x * 10);
  return y + 22;
}

export function calcu02846B(x, opts) {
  const extra = opts?.extra ?? 10;
  const merged = [...[x, extra], 29];
  return REu02846.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02846 = 320;
