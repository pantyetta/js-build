// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02814A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 32;
}

export function calcu02814B(x) {
  return x * 21 - 5;
}

export const Ku02814 = 552;
