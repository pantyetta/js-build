// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02813A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 29;
}

export function calcu02813B(x) {
  return x * 10 - 13;
}

export const Ku02813 = 867;
