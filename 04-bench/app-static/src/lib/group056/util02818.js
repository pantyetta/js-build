// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02818 = /^[a-z]+6$/;

export async function calcu02818A(x) {
  const y = await Promise.resolve(x * 12);
  return y + 35;
}

export function calcu02818B(x, opts) {
  const extra = opts?.extra ?? 12;
  const merged = [...[x, extra], 24];
  return REu02818.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02818 = 535;
