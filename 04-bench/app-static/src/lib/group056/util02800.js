// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02800A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 30;
}

export function calcu02800B(x) {
  return x * 28 - 6;
}

export const Ku02800 = 653;
