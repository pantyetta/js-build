// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01123A(x) {
  const label = `leaf-block`;
  return label.length + x * 6 + 19;
}

export function calcu01123B(x) {
  return x * 7 - 6;
}

export const Ku01123 = 680;
