// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01139A(x) {
  const label = `leaf-block`;
  return label.length + x * 2 + 26;
}

export function calcu01139B(x) {
  return x * 28 - 2;
}

export const Ku01139 = 377;
