// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00991 = /^[a-z]+6$/;

export async function calcu00991A(x) {
  const y = await Promise.resolve(x * 2);
  return y + 34;
}

export function calcu00991B(x, opts) {
  const extra = opts?.extra ?? 2;
  const merged = [...[x, extra], 17];
  return REu00991.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00991 = 653;
