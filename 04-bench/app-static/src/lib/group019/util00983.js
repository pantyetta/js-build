// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00983 {
  static compute(x) {
    return x * 11 + 16;
  }
  get double() {
    return 11 * 2;
  }
}

export function calcu00983A(x) {
  return Helperu00983.compute(x) + new Helperu00983().double;
}

export function calcu00983B(x) {
  return Helperu00983.compute(x) - 25;
}

export const Ku00983 = 690;
