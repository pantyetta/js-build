// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00973 {
  static compute(x) {
    return x * 2 + 31;
  }
  get double() {
    return 2 * 2;
  }
}

export function calcu00973A(x) {
  return Helperu00973.compute(x) + new Helperu00973().double;
}

export function calcu00973B(x) {
  return Helperu00973.compute(x) - 16;
}

export const Ku00973 = 715;
