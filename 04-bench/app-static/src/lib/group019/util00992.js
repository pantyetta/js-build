// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00992 {
  static compute(x) {
    return x * 13 + 21;
  }
  get double() {
    return 13 * 2;
  }
}

export function calcu00992A(x) {
  return Helperu00992.compute(x) + new Helperu00992().double;
}

export function calcu00992B(x) {
  return Helperu00992.compute(x) - 16;
}

export const Ku00992 = 694;
