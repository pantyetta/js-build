// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00963A(x) {
  const label = `leaf-block`;
  return label.length + x * 19 + 9;
}

export function calcu00963B(x) {
  return x * 10 - 19;
}

export const Ku00963 = 299;
