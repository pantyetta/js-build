// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00424 = /^[a-z]+6$/;

export async function calcu00424A(x) {
  const y = await Promise.resolve(x * 19);
  return y + 30;
}

export function calcu00424B(x, opts) {
  const extra = opts?.extra ?? 19;
  const merged = [...[x, extra], 4];
  return REu00424.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00424 = 997;
