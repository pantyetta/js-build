// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00440A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 3;
}

export function calcu00440B(x) {
  return x * 2 - 4;
}

export const Ku00440 = 77;
