// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00867 = /^[a-z]+6$/;

export async function calcu00867A(x) {
  const y = await Promise.resolve(x * 2);
  return y + 28;
}

export function calcu00867B(x, opts) {
  const extra = opts?.extra ?? 2;
  const merged = [...[x, extra], 8];
  return REu00867.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00867 = 714;
