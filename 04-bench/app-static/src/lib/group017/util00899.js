// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00899 {
  static compute(x) {
    return x * 16 + 2;
  }
  get double() {
    return 16 * 2;
  }
}

export function calcu00899A(x) {
  return Helperu00899.compute(x) + new Helperu00899().double;
}

export function calcu00899B(x) {
  return Helperu00899.compute(x) - 28;
}

export const Ku00899 = 280;
