// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00880A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 13;
}

export function calcu00880B(x) {
  return x * 29 - 18;
}

export const Ku00880 = 894;
