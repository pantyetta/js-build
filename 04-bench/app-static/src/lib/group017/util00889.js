// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00889 = /^[a-z]+6$/;

export async function calcu00889A(x) {
  const y = await Promise.resolve(x * 18);
  return y + 29;
}

export function calcu00889B(x, opts) {
  const extra = opts?.extra ?? 18;
  const merged = [...[x, extra], 17];
  return REu00889.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00889 = 966;
