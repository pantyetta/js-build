// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00877A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 30;
}

export function calcu00877B(x) {
  return x * 2 - 16;
}

export const Ku00877 = 309;
