// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00335 {
  static compute(x) {
    return x * 7 + 13;
  }
  get double() {
    return 7 * 2;
  }
}

export function calcu00335A(x) {
  return Helperu00335.compute(x) + new Helperu00335().double;
}

export function calcu00335B(x) {
  return Helperu00335.compute(x) - 8;
}

export const Ku00335 = 349;
