// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00313 = /^[a-z]+6$/;

export async function calcu00313A(x) {
  const y = await Promise.resolve(x * 19);
  return y + 7;
}

export function calcu00313B(x, opts) {
  const extra = opts?.extra ?? 19;
  const merged = [...[x, extra], 19];
  return REu00313.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00313 = 437;
