// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu00645A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 19;
}

export function calcu00645B(x) {
  return x * 23 - 4;
}

export const Ku00645 = 83;
