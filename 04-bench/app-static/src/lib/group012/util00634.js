// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu00634 {
  static compute(x) {
    return x * 15 + 31;
  }
  get double() {
    return 15 * 2;
  }
}

export function calcu00634A(x) {
  return Helperu00634.compute(x) + new Helperu00634().double;
}

export function calcu00634B(x) {
  return Helperu00634.compute(x) - 19;
}

export const Ku00634 = 304;
