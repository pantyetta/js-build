// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01308A(x) {
  const label = `leaf-block`;
  return label.length + x * 9 + 10;
}

export function calcu01308B(x) {
  return x * 11 - 9;
}

export const Ku01308 = 207;
