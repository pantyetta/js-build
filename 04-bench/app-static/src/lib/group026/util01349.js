// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01349A(x) {
  const label = `leaf-block`;
  return label.length + x * 13 + 12;
}

export function calcu01349B(x) {
  return x * 3 - 13;
}

export const Ku01349 = 558;
