// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02879A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 8;
}

export function calcu02879B(x) {
  return x * 14 - 7;
}

export const Ku02879 = 26;
