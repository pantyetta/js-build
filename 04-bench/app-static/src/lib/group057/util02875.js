// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02875 {
  static compute(x) {
    return x * 14 + 15;
  }
  get double() {
    return 14 * 2;
  }
}

export function calcu02875A(x) {
  return Helperu02875.compute(x) + new Helperu02875().double;
}

export function calcu02875B(x) {
  return Helperu02875.compute(x) - 28;
}

export const Ku02875 = 463;
