// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02882 = /^[a-z]+6$/;

export async function calcu02882A(x) {
  const y = await Promise.resolve(x * 10);
  return y + 27;
}

export function calcu02882B(x, opts) {
  const extra = opts?.extra ?? 10;
  const merged = [...[x, extra], 18];
  return REu02882.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02882 = 20;
