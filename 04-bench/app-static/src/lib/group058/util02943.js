// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02943A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 31;
}

export function calcu02943B(x) {
  return x * 21 - 7;
}

export const Ku02943 = 57;
