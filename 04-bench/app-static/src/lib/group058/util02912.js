// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02912A(x) {
  const label = `leaf-block`;
  return label.length + x * 17 + 35;
}

export function calcu02912B(x) {
  return x * 28 - 17;
}

export const Ku02912 = 867;
