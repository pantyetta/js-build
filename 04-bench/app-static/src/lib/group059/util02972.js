// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu02972 = /^[a-z]+6$/;

export async function calcu02972A(x) {
  const y = await Promise.resolve(x * 3);
  return y + 8;
}

export function calcu02972B(x, opts) {
  const extra = opts?.extra ?? 3;
  const merged = [...[x, extra], 25];
  return REu02972.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku02972 = 344;
