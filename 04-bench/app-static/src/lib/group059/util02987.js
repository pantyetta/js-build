// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02987A(x) {
  const label = `leaf-block`;
  return label.length + x * 15 + 18;
}

export function calcu02987B(x) {
  return x * 19 - 15;
}

export const Ku02987 = 726;
