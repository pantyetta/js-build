// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02964A(x) {
  const label = `leaf-block`;
  return label.length + x * 12 + 16;
}

export function calcu02964B(x) {
  return x * 25 - 12;
}

export const Ku02964 = 798;
