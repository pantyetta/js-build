// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02996 {
  static compute(x) {
    return x * 13 + 16;
  }
  get double() {
    return 13 * 2;
  }
}

export function calcu02996A(x) {
  return Helperu02996.compute(x) + new Helperu02996().double;
}

export function calcu02996B(x) {
  return Helperu02996.compute(x) - 22;
}

export const Ku02996 = 435;
