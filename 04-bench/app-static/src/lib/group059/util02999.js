// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02999A(x) {
  const label = `leaf-block`;
  return label.length + x * 5 + 33;
}

export function calcu02999B(x) {
  return x * 28 - 5;
}

export const Ku02999 = 2;
