// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu02978A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 9;
}

export function calcu02978B(x) {
  return x * 4 - 7;
}

export const Ku02978 = 657;
