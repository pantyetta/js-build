// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 2 — import なしの葉モジュール。

class Helperu02969 {
  static compute(x) {
    return x * 8 + 23;
  }
  get double() {
    return 8 * 2;
  }
}

export function calcu02969A(x) {
  return Helperu02969.compute(x) + new Helperu02969().double;
}

export function calcu02969B(x) {
  return Helperu02969.compute(x) - 28;
}

export const Ku02969 = 1;
