// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 5 — import なしの葉モジュール。

const REu00685 = /^[a-z]+6$/;

export async function calcu00685A(x) {
  const y = await Promise.resolve(x * 9);
  return y + 18;
}

export function calcu00685B(x, opts) {
  const extra = opts?.extra ?? 9;
  const merged = [...[x, extra], 16];
  return REu00685.test("leaf") ? merged.length + x : merged.length - x;
}

export const Ku00685 = 280;
