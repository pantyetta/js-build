// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01995A(x) {
  const label = `leaf-block`;
  return label.length + x * 16 + 6;
}

export function calcu01995B(x) {
  return x * 14 - 16;
}

export const Ku01995 = 436;
