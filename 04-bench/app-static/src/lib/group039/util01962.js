// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01962A(x) {
  const label = `leaf-block`;
  return label.length + x * 8 + 12;
}

export function calcu01962B(x) {
  return x * 14 - 8;
}

export const Ku01962 = 257;
