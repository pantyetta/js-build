// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01959A(x) {
  const label = `leaf-block`;
  return label.length + x * 18 + 31;
}

export function calcu01959B(x) {
  return x * 2 - 18;
}

export const Ku01959 = 832;
