// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01966A(x) {
  const label = `leaf-block`;
  return label.length + x * 7 + 20;
}

export function calcu01966B(x) {
  return x * 4 - 7;
}

export const Ku01966 = 632;
