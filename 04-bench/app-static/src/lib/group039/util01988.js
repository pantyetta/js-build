// 生成ファイル（04-bench/generate.mjs）。手で編集しない。
// variant 0 — import なしの葉モジュール。

export function calcu01988A(x) {
  const label = `leaf-block`;
  return label.length + x * 4 + 13;
}

export function calcu01988B(x) {
  return x * 4 - 4;
}

export const Ku01988 = 508;
